# 종별 9종 확정 · 확인 화면 2항목 · 전월 결과파일 이력 누적 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 근무상황 종별을 9종 완전일치로 확정하고, 그 밖의 표기·사유가 적힌 행은 사람이 유급/무급과 주휴·연가 발생을 건별로 정하며, 전월 결과파일이 과거 근무상황과 그 판정을 날라 당월분만 첨부해도 소급계산이 되게 한다.

**Architecture:** 급여·만근 판정을 `classified` 문자열 비교에서 `LeaveEvent`의 두 불리언 축(`unpaid`, `breaks`)으로 옮긴다. 규칙이 채우는 값과 사람이 채우는 값이 같은 모양이라 엔진은 둘을 구분하지 않는다. 이력 누적은 `core/parser.py` 한 곳에서 전월 결과파일의 `근무상황(기간제)` 시트를 당월 B파일과 합쳐 `giganje_rows` 하나로 만들어 기존 파이프라인에 흘린다.

**Tech Stack:** Python 3, openpyxl, FastAPI + pywebview, pytest, 순수 ES 모듈 프론트엔드

**Spec:** `docs/superpowers/specs/2026-09-01-종별-정리-및-이력누적-design.md`

## Global Constraints

- 작업 폴더는 `C:\Users\seong\Desktop\기간제임금`, 브랜치는 `fix/종별인식-및-근무상황기간검증`이다.
- 테스트 실행: `python -m pytest wage_calculator/tests -q` (저장소 루트에서).
- 인식 종별은 정확히 9개: `연가` `반일연가` `공가` `일반병가` `결근` `조퇴` `외출` `지각` `기타`.
- 보유 연가를 소진·상계하는 종별은 `연가` `반일연가` `조퇴` `외출` `지각` 다섯. `기타`는 상계하지 않는다.
- 판정 열 이름은 정확히 `지급판정`(값 `유급`/`무급`)과 `주휴연가판정`(값 `발생`/`미발생`).
- 확인 화면의 답은 `unpaid`/`breaks`만 채운다. **인식된 9종의 `classified`는 절대 덮어쓰지 않는다** — 덮어쓰면 `연가`의 잔량 소진이 사라진다.
- 커밋 메시지는 한국어 한 줄 요약 + 기존 관례(`feat:`/`fix:`/`refactor:`/`test:`/`docs:`).
- `git commit` 은 항상 경로를 지정한다(`git commit -- <경로>`). `git add -A` 금지.

## 설계 대비 조정 (구현 중 확정)

1. **`LeaveEvent.source_row`** — 원본 B파일 행 dict 참조를 이벤트에 싣는다. 확인 화면이 판정을 원본 행에 되쓸 때 이름·종별·기간으로 재매칭할 필요가 없어지고, `webapp/state.py:_find_reason_note()`와 `gui/special_leave_screen.py`의 같은 함수가 삭제된다.
2. **급여 공제를 `unpaid` 한 축으로 통일** — `payroll.NON_DEDUCTIBLE` 튜플을 없앤다. "이 항목의 시간·일수를 급여에서 깎는가"는 `e.unpaid` 하나로 답한다.
3. **`breaks`는 시간 기재 여부와 무관한 축** — "이 이벤트는 그날을 실근무 0분으로 만든다"는 뜻. 규칙은 종일 `결근`·`일반병가`·`기타`에만 True를 넣고, 사람이 `발생 안 함`을 고르면 시간 기재 건에도 True가 될 수 있다.
4. **확인 대상 판정을 `core/pending.py`로 추출** — 화면이 아니라 순수 로직이므로 `core/`에 둔다.
5. **`gui/`(tkinter 화면) 삭제** — v5.0에서 pywebview 웹 UI로 전환한 뒤 실행 경로에서 빠졌다. `main.py`도 exe 빌드 스펙(`통계조사관임금계산.spec`)도 `gui/`를 참조하지 않고, 남은 참조는 테스트 3개의 import뿐이다. 확인 화면이 2항목으로 바뀌면 같은 규칙을 두 벌 유지해야 하므로 이번에 지운다(필요하면 git 이력에서 되살린다). 사용자 결정.

## File Structure

**신규**

| 파일 | 책임 |
|---|---|
| `wage_calculator/core/pending.py` | 확인 화면에 올릴 그룹 수집 + 사람 판정 적용(순수 로직, 화면 없음) |
| `wage_calculator/tests/test_mapping_known_categories.py` | 9종 완전일치 규칙 고정 |
| `wage_calculator/tests/test_payroll_gita.py` | `기타` 종일/부분시간 급여·식대·만근 고정 |
| `wage_calculator/tests/test_pending_decisions.py` | 확인 대상 수집·기본값·판정 적용 고정 |
| `wage_calculator/tests/test_parser_status_history.py` | 전월 근무상황 읽기 + 합치기 고정 |
| `wage_calculator/tests/test_status_history_roundtrip.py` | 계산 → 저장 → 전월 파일로 재로드 라운드트립 |

**수정**

| 파일 | 변경 |
|---|---|
| `core/mapping.py` | 키워드 규칙·예시표 삭제, 9종 완전일치 + 성질 상수표 |
| `core/models.py` | `LeaveEvent`에 `unpaid`/`source_row`/`decided` 추가, `breaks` 되살림, `build_event` 규칙 적용 |
| `core/leave_engine.py` | 만근 판정을 `breaks` 기준으로, 상계 대상을 `조퇴/외출/지각`으로 |
| `core/payroll.py` | `absence_days`·급여 공제를 `unpaid` 기준으로, 상계 대상 한정 |
| `core/parser.py` | `events_from_row`가 판정 열을 읽음, 전월 근무상황 로더·합치기 신설 |
| `core/coverage.py` | 안내문에 전월 결과파일 첨부 안내 추가 |
| `output/raw_sheet.py` | 판정 2열 추가 + 열 이름 상수 공개 |
| `webapp/state.py` | 합본 배선, `core.pending` 사용, 확인 API 2항목화 |
| `webapp/server.py` | 확인 요청 모델 2항목화 |
| `webapp/static/js/specialLeave.js` | 확인 화면 2항목 토글 |
| `README.md`, `변경이력.txt`, `임금계산_로직_안내(v5.3).txt` | 문서 갱신 |

**삭제**

| 파일 | 이유 |
|---|---|
| `wage_calculator/gui/` (10개 파일) | 실행 경로에서 빠진 tkinter 화면. 같은 규칙을 두 벌 유지하지 않는다 |
| `tests/test_mapping_normalization.py`, `tests/test_mapping_special_leave.py` | 삭제된 키워드 규칙·예시표만 검증 |
| `tests/test_pending_category_groups.py` | 웹/GUI 두 수집기를 비교하는 테스트. 수집기가 하나가 되어 목적이 사라진다(같은 단정은 `test_pending_decisions.py`가 잇는다) |

---

### Task 0: 죽은 tkinter 화면(`gui/`)을 지운다

**Files:**
- Delete: `wage_calculator/gui/` 전체, `wage_calculator/tests/test_pending_category_groups.py`
- Modify: `wage_calculator/tests/test_end_to_end_v3.py:15`, `wage_calculator/tests/test_special_leave_grouping.py:8`

**Interfaces:**
- Consumes: 없음
- Produces: 확인 대상 수집기가 `webapp.state.collect_pending_groups` 하나만 남는다(Task 5에서 `core.pending`으로 옮긴다)

- [ ] **Step 1: 실행 경로가 `gui/`를 쓰지 않는지 확인한다**

```bash
grep -rn "from gui\|import gui" --include=*.py wage_calculator | grep -v "wage_calculator/tests/" | grep -v "wage_calculator/gui/"
grep -n "gui" 통계조사관임금계산.spec
```
Expected: 두 명령 모두 출력이 없다. 출력이 있으면 여기서 멈추고 사용자에게 알린다.

- [ ] **Step 2: 테스트 import를 웹 쪽 수집기로 돌린다**

`wage_calculator/tests/test_end_to_end_v3.py:15`:

```python
from webapp.state import collect_pending_groups
```

`wage_calculator/tests/test_special_leave_grouping.py:8`:

```python
from webapp.state import collect_pending_groups
```

- [ ] **Step 3: 두 수집기를 비교하던 테스트를 지운다**

`wage_calculator/tests/test_pending_category_groups.py`는 웹과 GUI 수집기가 같은 결과를 내는지 확인하는 테스트다. 수집기가 하나가 되면 검증할 대상이 없어진다. 같은 단정(미인식 종별 수집, 인식 종별 미수집, 같은 기간 다른 종별 분리)은 Task 5의 `test_pending_decisions.py`가 잇는다.

```bash
git rm wage_calculator/tests/test_pending_category_groups.py
```

- [ ] **Step 4: `gui/`를 지운다**

```bash
git rm -r wage_calculator/gui
```

- [ ] **Step 5: 전체 테스트가 그대로 통과하는지 확인한다**

Run: `python -m pytest wage_calculator/tests -q`
Expected: 전부 PASS(삭제한 파일 수만큼 개수가 준다). 실패하면 `gui` import가 남은 곳을 찾아 고친다.

- [ ] **Step 6: 앱이 뜨는지 확인한다**

Run: `python wage_calculator/main.py`
Expected: 창이 뜨고 업로드 화면이 보인다. 확인 후 닫는다.

- [ ] **Step 7: 커밋**

```bash
git commit -m "chore: 실행 경로에서 빠진 tkinter 화면(gui/) 삭제" -- wage_calculator/gui wage_calculator/tests/test_end_to_end_v3.py wage_calculator/tests/test_special_leave_grouping.py wage_calculator/tests/test_pending_category_groups.py
```

---

### Task 1: 종별 9종 완전일치

**Files:**
- Modify: `wage_calculator/core/mapping.py` (전면 교체)
- Test: `wage_calculator/tests/test_mapping_known_categories.py` (신규)
- Delete: `wage_calculator/tests/test_mapping_normalization.py`, `wage_calculator/tests/test_mapping_special_leave.py`

**Interfaces:**
- Consumes: 없음
- Produces: `mapping.KNOWN_CATEGORIES`, `mapping.UNPAID`, `mapping.BREAKS_WHEN_FULL_DAY`, `mapping.OFFSETTABLE`, `mapping.LEAVE_CONSUMING`, `mapping.UNDETERMINED_SPECIAL`, `mapping.DECIDED_SPECIAL`, `mapping.normalize(raw)`, `mapping.classify(raw)`, `mapping.is_pending(classified)`, `mapping.full_day_weight(raw)`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`wage_calculator/tests/test_mapping_known_categories.py`:

```python
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

from core import mapping


def test_nine_known_categories_classify_to_themselves():
    for name in ("연가", "반일연가", "공가", "일반병가", "결근", "조퇴", "외출", "지각", "기타"):
        assert mapping.classify(name) == name


def test_full_width_and_padding_are_normalized():
    assert mapping.classify("  연가 ") == "연가"
    assert mapping.classify("연가") == "연가"          # 이미 반각
    assert mapping.classify("\u1100" ) == mapping.UNDETERMINED_SPECIAL


def test_parenthesised_variants_go_to_pending():
    for name in ("일반병가(진단서미첨부)", "조퇴(연가처리)", "반일연가(오전)",
                 "공무상병가", "관외여행", "경조사휴가", "자녀돌봄휴가", "대체휴무"):
        assert mapping.classify(name) == mapping.UNDETERMINED_SPECIAL


def test_empty_and_none_go_to_pending():
    assert mapping.classify(None) == mapping.UNDETERMINED_SPECIAL
    assert mapping.classify("") == mapping.UNDETERMINED_SPECIAL
    assert mapping.classify("   ") == mapping.UNDETERMINED_SPECIAL


def test_classify_never_raises():
    for value in (123, object(), ["연가"]):
        assert mapping.classify(value) == mapping.UNDETERMINED_SPECIAL


def test_property_tables():
    assert mapping.UNPAID == ("결근", "조퇴", "외출", "지각", "기타")
    assert mapping.BREAKS_WHEN_FULL_DAY == ("결근", "일반병가", "기타")
    assert mapping.OFFSETTABLE == ("조퇴", "외출", "지각")
    assert mapping.LEAVE_CONSUMING == ("연가", "반일연가", "조퇴", "외출", "지각")


def test_half_day_annual_leave_weight():
    assert mapping.full_day_weight("반일연가") == 0.5
    assert mapping.full_day_weight("연가") == 1.0
    assert mapping.full_day_weight("기타") == 1.0


def test_is_pending():
    assert mapping.is_pending(mapping.UNDETERMINED_SPECIAL) is True
    assert mapping.is_pending("연가") is False
    assert mapping.is_pending("유급특별휴가") is False


if __name__ == "__main__":
    test_nine_known_categories_classify_to_themselves()
    test_full_width_and_padding_are_normalized()
    test_parenthesised_variants_go_to_pending()
    test_empty_and_none_go_to_pending()
    test_classify_never_raises()
    test_property_tables()
    test_half_day_annual_leave_weight()
    test_is_pending()
    print("ALL OK")
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest wage_calculator/tests/test_mapping_known_categories.py -q`
Expected: FAIL — `AttributeError: module 'core.mapping' has no attribute 'KNOWN_CATEGORIES'`

- [ ] **Step 3: `core/mapping.py`를 통째로 교체한다**

```python
"""근무상황 "종별" 판정.

기간제 조사관 근무상황에 실제로 찍히는 종별은 아래 9개뿐이다. 정규화(전각->반각,
앞뒤 공백 제거) 후 이 9개와 **완전일치**할 때만 인식하고, 그 밖의 표기는 예외를
던지지 않고 전부 확인 화면으로 보낸다(사람이 유급/무급과 주휴·연가 발생을 고른다).

예전에는 청·지청별 표기 차이를 키워드 우선순위 규칙으로 흡수했다. 그 결과
조퇴·외출·지각이 기타와 같은 분류가 되어 "기타는 연가로 상계하지 않는다"를
표현할 수 없었다. 표기를 넓게 받아들이는 대신, 벗어나면 사람이 판단한다.

급여·만근 처리는 여기서 문자열로 결정되지 않는다. 이 모듈은 종별의 '성질'만
표로 제공하고, 실제 값은 models.build_event()가 이벤트의 두 축(unpaid/breaks)에
싣는다 - 사람이 확인 화면에서 채우는 값과 같은 모양이라야 엔진이 둘을 구분하지
않기 때문이다.
"""
import unicodedata

# 유급/무급이 종별만으로 정해지지 않아, 사람이 확인 화면에서 결정해야 하는 분류.
UNDETERMINED_SPECIAL = "특별휴가_미정"
PENDING_CLASSIFICATIONS = (UNDETERMINED_SPECIAL,)

# 사람이 고른 뒤 미인식 종별에만 덮어쓰는 표시용 분류(인식된 9종은 덮지 않는다).
DECIDED_SPECIAL = ("유급특별휴가", "무급특별휴가")

KNOWN_CATEGORIES = (
    "연가", "반일연가", "공가", "일반병가", "결근", "조퇴", "외출", "지각", "기타",
)

# 그 시간·일수를 급여에서 깎는 종별(조퇴/외출/지각은 보유 연가로 상계된 만큼은 깎이지 않는다).
UNPAID = ("결근", "조퇴", "외출", "지각", "기타")

# 사용시간 없이 종일로 찍히면 그날을 실근무 0분으로 보는 종별(만근이 깨진다).
BREAKS_WHEN_FULL_DAY = ("결근", "일반병가", "기타")

# 시간 기재분이 보유 연가로 상계되는 종별.
OFFSETTABLE = ("조퇴", "외출", "지각")

# 보유 연가를 소진하거나 상계하는 종별 전체(연가·반일연가는 직접 소진).
LEAVE_CONSUMING = ("연가", "반일연가") + OFFSETTABLE


def normalize(raw_category) -> str:
    """전각->반각(NFKC) + 앞뒤 공백 제거. 괄호·쉼표 같은 구분자는 지우지 않는다 -
    지우면 "조퇴(연가처리)"가 9종 어디에도 안 맞는다는 사실이 흐려진다."""
    try:
        text = str(raw_category or "")
    except Exception:
        return ""
    return unicodedata.normalize("NFKC", text).strip()


def classify(raw_category) -> str:
    """종별 원문 -> 9종 중 하나 또는 "특별휴가_미정". 절대 예외를 던지지 않는다."""
    normalized = normalize(raw_category)
    return normalized if normalized in KNOWN_CATEGORIES else UNDETERMINED_SPECIAL


def is_pending(classified: str) -> bool:
    """사람이 판정해야 계산을 진행할 수 있는 분류인지."""
    return classified in PENDING_CLASSIFICATIONS


def full_day_weight(raw_category) -> float:
    """사용시간(시분) 미기재(종일) 항목의 일수 가중치(반일연가는 0.5일)."""
    return 0.5 if classify(raw_category) == "반일연가" else 1.0
```

- [ ] **Step 4: 통과를 확인한다**

Run: `python -m pytest wage_calculator/tests/test_mapping_known_categories.py -q`
Expected: PASS (8 passed)

- [ ] **Step 5: 낡은 매핑 테스트를 지운다**

두 파일은 삭제된 키워드 규칙·예시표(`classify_by_rules`, `CATEGORY_MAP`, `full_day_breaks`)만 검증한다. 새 규칙에서는 존재 자체가 모순이므로 갱신이 아니라 삭제한다.

```bash
git rm wage_calculator/tests/test_mapping_normalization.py wage_calculator/tests/test_mapping_special_leave.py
```

- [ ] **Step 6: 커밋**

```bash
git add wage_calculator/core/mapping.py wage_calculator/tests/test_mapping_known_categories.py
git commit -m "refactor: 종별 판정을 9종 완전일치로 좁히고 성질 표로 분리" -- wage_calculator/core/mapping.py wage_calculator/tests/test_mapping_known_categories.py wage_calculator/tests/test_mapping_normalization.py wage_calculator/tests/test_mapping_special_leave.py
```

이 시점에서 다른 테스트는 아직 깨진 상태다(다음 태스크에서 맞춘다).

---

### Task 2: `LeaveEvent`에 판정 두 축을 싣는다

**Files:**
- Modify: `wage_calculator/core/models.py`
- Test: `wage_calculator/tests/test_models_event_axes.py` (신규)

**Interfaces:**
- Consumes: Task 1의 `mapping.UNPAID`, `mapping.BREAKS_WHEN_FULL_DAY`, `mapping.classify`, `mapping.full_day_weight`
- Produces: `LeaveEvent.unpaid: bool`, `LeaveEvent.breaks: bool`, `LeaveEvent.decided: bool`, `LeaveEvent.source_row: Optional[dict]`, `build_event(raw_category, d, time_start=None, time_end=None, minutes=0, source_range=None, source_row=None) -> LeaveEvent`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`wage_calculator/tests/test_models_event_axes.py`:

```python
import sys
from pathlib import Path
from datetime import date, time

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

from core.models import build_event


def test_full_day_gita_is_unpaid_and_breaks_attendance():
    """종일 기타는 결근과 같은 모양 - 일급·식대 미지급 + 만근 깨짐."""
    e = build_event("기타", date(2026, 8, 10))
    assert e.unpaid is True
    assert e.breaks is True
    assert e.day_weight == 1.0
    assert e.is_time_based is False


def test_time_based_gita_deducts_but_keeps_attendance():
    e = build_event("기타", date(2026, 8, 10), time(9, 0), time(12, 0), 180)
    assert e.unpaid is True
    assert e.breaks is False
    assert e.day_weight == 0.0


def test_full_day_sick_leave_is_paid_but_breaks_attendance():
    e = build_event("일반병가", date(2026, 8, 10))
    assert e.unpaid is False
    assert e.breaks is True


def test_annual_and_public_leave_keep_everything():
    for name in ("연가", "반일연가", "공가"):
        e = build_event(name, date(2026, 8, 10))
        assert e.unpaid is False, name
        assert e.breaks is False, name


def test_absence_is_unpaid_and_breaks():
    e = build_event("결근", date(2026, 8, 10))
    assert e.unpaid is True
    assert e.breaks is True


def test_time_based_early_leave_is_unpaid_but_not_breaking():
    e = build_event("조퇴", date(2026, 8, 10), time(14, 0), time(18, 0), 240)
    assert e.unpaid is True
    assert e.breaks is False


def test_unknown_category_starts_undecided_and_neutral():
    e = build_event("경조사휴가", date(2026, 8, 10))
    assert e.classified == "특별휴가_미정"
    assert e.unpaid is False
    assert e.breaks is False
    assert e.decided is False


def test_source_row_is_carried():
    row = {"성명": "홍길동", "종별": "경조사휴가"}
    e = build_event("경조사휴가", date(2026, 8, 10), source_row=row)
    assert e.source_row is row


if __name__ == "__main__":
    test_full_day_gita_is_unpaid_and_breaks_attendance()
    test_time_based_gita_deducts_but_keeps_attendance()
    test_full_day_sick_leave_is_paid_but_breaks_attendance()
    test_annual_and_public_leave_keep_everything()
    test_absence_is_unpaid_and_breaks()
    test_time_based_early_leave_is_unpaid_but_not_breaking()
    test_unknown_category_starts_undecided_and_neutral()
    test_source_row_is_carried()
    print("ALL OK")
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest wage_calculator/tests/test_models_event_axes.py -q`
Expected: FAIL — `AttributeError: 'LeaveEvent' object has no attribute 'unpaid'`

- [ ] **Step 3: `core/models.py`의 `LeaveEvent`와 `build_event`를 고친다**

`LeaveEvent` 필드 부분을 아래로 교체한다(다른 dataclass는 그대로 둔다):

```python
@dataclass
class LeaveEvent:
    """근무상황 원본 1행이 펼쳐진, 특정 하루에 대한 사건 1건.

    급여·만근 처리는 종별 문자열이 아니라 아래 두 축이 결정한다. 규칙이 채우는
    값과 사람이 확인 화면에서 채우는 값이 같은 모양이라, 엔진은 둘을 구분하지
    않는다.
    """
    raw_category: str
    classified: str          # 9종 중 하나 / 특별휴가_미정 / 유급특별휴가 / 무급특별휴가
    d: date
    is_time_based: bool
    time_start: Optional[time] = None
    time_end: Optional[time] = None
    minutes: int = 0          # 조퇴/외출/지각/기타 공제 분
    day_weight: float = 1.0   # 종일 기준 일수(반일연가=0.5, 시간 기재=0)
    unpaid: bool = False      # 그 시간·일수를 급여에서 깎는가(식대도 종일이면 같이 빠진다)
    breaks: bool = False      # 그날을 실근무 0분으로 보는가(주휴·연가 만근이 깨진다)
    decided: bool = False     # 전월 결과파일에서 사람 판정을 이어받았는가(확인 화면에 다시 올리지 않는다)
    source_range: Optional[Tuple[date, date]] = None  # (원본 행의 사용기간 시작일, 종료일)
    source_row: Optional[dict] = None                 # 원본 B파일 행 dict 참조(확인 화면이 판정을 되쓴다)
```

`build_event`를 아래로 교체한다:

```python
def build_event(raw_category: str, d: date, time_start=None, time_end=None, minutes: int = 0,
                source_range=None, source_row=None) -> LeaveEvent:
    classified = mapping.classify(raw_category)
    # 사용시간(시분)이 실제로 기재된 행이면 종별 무관하게 시간공제 대상이다.
    time_based = time_start is not None
    weight = 0.0 if time_based else mapping.full_day_weight(raw_category)
    unpaid = classified in mapping.UNPAID
    # 종일(시간 미기재)로 찍힌 결근·일반병가·기타만 그날을 통째로 비운 것으로 본다.
    # 시간 기재 건이 8시간을 다 채우는지는 leave_engine이 분 단위로 따로 판정한다.
    breaks = (not time_based) and classified in mapping.BREAKS_WHEN_FULL_DAY
    return LeaveEvent(
        raw_category=raw_category,
        classified=classified,
        d=d,
        is_time_based=time_based,
        time_start=time_start,
        time_end=time_end,
        minutes=minutes if time_based else 0,
        day_weight=weight,
        unpaid=unpaid,
        breaks=breaks,
        source_range=source_range or (d, d),
        source_row=source_row,
    )
```

- [ ] **Step 4: 통과를 확인한다**

Run: `python -m pytest wage_calculator/tests/test_models_event_axes.py -q`
Expected: PASS (8 passed)

- [ ] **Step 5: 커밋**

```bash
git commit -m "feat: LeaveEvent에 급여(unpaid)·만근(breaks) 두 축과 원본 행 참조 추가" -- wage_calculator/core/models.py wage_calculator/tests/test_models_event_axes.py
```

---

### Task 3: 만근·연가 판정을 새 축으로 옮긴다

**Files:**
- Modify: `wage_calculator/core/leave_engine.py:47-66`(`_worked_minutes_by_day`), `:130-146`(`_consumes_leave_candidate`, `leave_usage_minutes`), `:224-260`(`simulate_leave_consumption`)
- Test: `wage_calculator/tests/test_leave_engine_gita.py` (신규)

**Interfaces:**
- Consumes: Task 2의 `LeaveEvent.breaks`, Task 1의 `mapping.OFFSETTABLE`
- Produces: 변경 없음(기존 함수 시그니처 유지). 동작만 바뀐다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`wage_calculator/tests/test_leave_engine_gita.py`:

```python
import sys
from pathlib import Path
from datetime import date, time

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

from core.leave_engine import (
    compute_monthly_leave_windows,
    compute_weekly_holiday_windows,
    simulate_leave_consumption,
    weekly_holidays_for_month,
)
from core.models import build_event

START = date(2026, 7, 1)
END = date(2026, 9, 30)


def _weekly(events):
    return compute_weekly_holiday_windows(START, END, events)


def test_full_day_gita_breaks_that_week_holiday():
    """2026-08-10(월) 종일 기타 -> 그 주 창의 주휴가 발생하지 않는다."""
    events = [build_event("기타", date(2026, 8, 10))]
    granted = [w for w in _weekly(events) if w.start <= date(2026, 8, 10) <= w.effective_end]
    assert granted and granted[0].granted is False


def test_full_day_gita_breaks_monthly_full_attendance():
    events = [build_event("기타", date(2026, 8, 10))]
    windows = compute_monthly_leave_windows(START, END, events)
    hit = [w for w in windows if w.start <= date(2026, 8, 10) <= w.effective_end]
    assert hit and hit[0].accrued is False


def test_partial_gita_keeps_full_attendance():
    events = [build_event("기타", date(2026, 8, 10), time(9, 0), time(12, 0), 180)]
    windows = compute_monthly_leave_windows(START, END, events)
    hit = [w for w in windows if w.start <= date(2026, 8, 10) <= w.effective_end]
    assert hit and hit[0].accrued is True
    assert weekly_holidays_for_month(_weekly(events), 2026, 8) > 0


def test_gita_is_not_offset_by_remaining_leave():
    """7월 만근으로 8/1 연가 발생이 있어도 기타는 상계되지 않는다."""
    e = build_event("기타", date(2026, 8, 10), time(9, 0), time(12, 0), 180)
    windows = compute_monthly_leave_windows(START, END, [e])
    consumption = simulate_leave_consumption(windows, [e])
    assert consumption.total_offset_minutes == 0
    assert consumption.offset_by_id == {}


def test_early_leave_is_still_offset():
    e = build_event("조퇴", date(2026, 8, 10), time(15, 0), time(18, 0), 180)
    windows = compute_monthly_leave_windows(START, END, [e])
    consumption = simulate_leave_consumption(windows, [e])
    assert consumption.total_offset_minutes == 180


def test_full_day_sick_leave_still_breaks_attendance():
    events = [build_event("일반병가", date(2026, 8, 10))]
    windows = compute_monthly_leave_windows(START, END, events)
    hit = [w for w in windows if w.start <= date(2026, 8, 10) <= w.effective_end]
    assert hit and hit[0].accrued is False


if __name__ == "__main__":
    test_full_day_gita_breaks_that_week_holiday()
    test_full_day_gita_breaks_monthly_full_attendance()
    test_partial_gita_keeps_full_attendance()
    test_gita_is_not_offset_by_remaining_leave()
    test_early_leave_is_still_offset()
    test_full_day_sick_leave_still_breaks_attendance()
    print("ALL OK")
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest wage_calculator/tests/test_leave_engine_gita.py -q`
Expected: FAIL — 종일 기타가 만근을 깨지 않아 `test_full_day_gita_breaks_that_week_holiday`·`test_full_day_gita_breaks_monthly_full_attendance`가 실패하고, 기타가 상계되어 `test_gita_is_not_offset_by_remaining_leave`가 실패한다.

- [ ] **Step 3: `core/leave_engine.py`를 고친다**

파일 상단 import에 `mapping`을 추가한다:

```python
from . import date_utils, mapping
```

`_worked_minutes_by_day()`의 종일 판정을 바꾼다:

```python
def _worked_minutes_by_day(window_events, w_start: date, eff_end: date) -> dict:
    """창 내 월~금 각 날짜의 실근무시간(분).

    그날을 통으로 비운 이벤트(e.breaks - 종일 결근/일반병가/기타, 또는 확인 화면에서
    사람이 '주휴·연가 발생 안 함'을 고른 건)가 있으면 0분. 그 외의 날은 소정근로
    480분에서 그날의 시간 기재분(사유 무관)만큼만 뺀다. 8시간(480분) 전부가
    공제되면 그날은 사실상 출근하지 않은 것과 같아 0분이 된다.
    """
    by_day = {}
    for d in date_utils.daterange(w_start, eff_end):
        if d.weekday() >= 5:
            continue
        day_events = [e for e in window_events if e.d == d]
        if any(e.breaks for e in day_events):
            by_day[d] = 0
        else:
            deducted = sum(e.minutes for e in day_events if e.is_time_based)
            by_day[d] = max(480 - deducted, 0)
    return by_day
```

`_consumes_leave_candidate()`를 바꾼다:

```python
def _consumes_leave_candidate(event) -> bool:
    """연가 잔량을 소진하는 후보: 연가/반일연가 + 시간 기재 조퇴/외출/지각.

    "기타"는 제외한다 - 기간제 규칙상 기타는 연가 보유 여부와 무관하게 급여에서
    깎는다(상계하면 연가가 조용히 줄어든다).

    결근도 제외한다. e-사람 안내표에는 결근이 연가일수 '공제'로 되어 있으나 그것은
    공무원 복무 기준이고, 여기서는 결근이 그날 일급·정액급식비를 아예 지급하지
    않는다(payroll.absence_days). 급여를 깎으면서 연가까지 깎으면 같은 하루로 두 번
    불이익을 주게 된다.
    """
    if event.classified in ("연가", "반일연가"):
        return True
    return event.is_time_based and event.classified in mapping.OFFSETTABLE
```

`leave_usage_minutes()`의 마지막 조건을 바꾼다:

```python
    if offset_map is not None and event.is_time_based and event.classified in mapping.OFFSETTABLE:
        return offset_map.get(id(event), 0)
```

`simulate_leave_consumption()`의 항목 수집 분기를 바꾼다:

```python
        elif e.is_time_based and e.classified in mapping.OFFSETTABLE:
            entries.append((e.d, 2, e.minutes, e))
```

같은 함수의 docstring에서 "조퇴/외출/지각(시간 기재 '기타')"를 "조퇴/외출/지각(시간 기재)"로 고친다.

- [ ] **Step 4: 통과를 확인한다**

Run: `python -m pytest wage_calculator/tests/test_leave_engine_gita.py -q`
Expected: PASS (6 passed)

- [ ] **Step 5: 기존 leave_engine 테스트가 그대로 통과하는지 본다**

Run: `python -m pytest wage_calculator/tests/test_leave_engine_weekly.py wage_calculator/tests/test_leave_engine_monthly.py wage_calculator/tests/test_leave_engine_simulate.py wage_calculator/tests/test_leave_engine_balance_offset.py wage_calculator/tests/test_leave_engine_offset_usage.py -q`
Expected: 실패가 남으면, 그 테스트가 `"병가"`·`"기타"` 같은 옛 분류 문자열을 쓰고 있는지 확인한다. `"병가"` → `"일반병가"`로, 상계를 기대하는 `"기타"` → `"조퇴"`로 바꾼다(둘 다 표기만 바뀌고 의도는 같다). 상계를 기대하지 않는 `"기타"`는 그대로 두고 기대값을 새 규칙에 맞춘다.

- [ ] **Step 6: 커밋**

```bash
git commit -m "feat: 만근 판정을 breaks 축으로, 연가 상계를 조퇴·외출·지각으로 한정" -- wage_calculator/core/leave_engine.py wage_calculator/tests/
```

---

### Task 4: 급여·식대를 `unpaid` 축으로 통일한다

**Files:**
- Modify: `wage_calculator/core/payroll.py:73-77`(집계), `:110-131`(시간 공제·상계)
- Test: `wage_calculator/tests/test_payroll_gita.py` (신규)

**Interfaces:**
- Consumes: Task 2의 `LeaveEvent.unpaid`, Task 1의 `mapping.OFFSETTABLE`
- Produces: `PayrollResult` 필드 구성 변경 없음

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`wage_calculator/tests/test_payroll_gita.py`:

```python
import sys
from pathlib import Path
from datetime import date, time
from types import SimpleNamespace

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

from core.config import Config
from core.models import build_event
from core.payroll import calc_payroll


def _config():
    return Config({"surveys": [], "rates": {"2026": {"daily_wage": 78560, "meal_allowance": 160000}}, "holidays": []})


def _person(events, start=date(2026, 7, 1), end=date(2026, 9, 30)):
    return SimpleNamespace(
        contract_start=start, contract_end=end, events=events,
        name="테스트", birth="", ssn="", bank="", account="", survey_name="테스트조사",
    )


def test_full_day_gita_matches_absence():
    """종일 기타 1일 = 결근 1일. 급여일수·식대해당일이 같아야 한다."""
    gita = calc_payroll(_person([build_event("기타", date(2026, 8, 10))]), _config(), 2026, 8)
    absent = calc_payroll(_person([build_event("결근", date(2026, 8, 10))]), _config(), 2026, 8)
    assert gita.absence_days == 1.0
    assert gita.total_days == absent.total_days
    assert gita.meal_eligible_days == absent.meal_eligible_days
    assert gita.total_payment == absent.total_payment


def test_full_day_gita_removes_that_week_holiday():
    baseline = calc_payroll(_person([]), _config(), 2026, 8)
    gita = calc_payroll(_person([build_event("기타", date(2026, 8, 10))]), _config(), 2026, 8)
    assert gita.weekly_holiday_days == baseline.weekly_holiday_days - 1


def test_partial_gita_deducts_time_only_and_keeps_meal():
    baseline = calc_payroll(_person([]), _config(), 2026, 8)
    e = build_event("기타", date(2026, 8, 10), time(9, 0), time(12, 0), 180)
    r = calc_payroll(_person([e]), _config(), 2026, 8)
    assert r.late_out_minutes == 180
    assert r.leave_offset_minutes == 0          # 상계 없음
    assert r.absence_days == 0
    assert r.meal_eligible_days == baseline.meal_eligible_days
    assert r.weekly_holiday_days == baseline.weekly_holiday_days


def test_partial_gita_does_not_touch_remaining_leave():
    """8월 시점 보유 연가(7월 만근분 1일)가 기타 때문에 줄지 않는다."""
    baseline = calc_payroll(_person([]), _config(), 2026, 8)
    e = build_event("기타", date(2026, 8, 10), time(9, 0), time(12, 0), 180)
    r = calc_payroll(_person([e]), _config(), 2026, 8)
    assert r.remaining_leave_days == baseline.remaining_leave_days


def test_early_leave_is_still_offset_not_deducted():
    e = build_event("조퇴", date(2026, 8, 10), time(15, 0), time(18, 0), 180)
    r = calc_payroll(_person([e]), _config(), 2026, 8)
    assert r.leave_offset_minutes == 180
    assert r.late_out_minutes == 0
    assert r.late_out_deduction == 0


def test_full_day_sick_leave_is_paid_but_loses_weekly_holiday():
    baseline = calc_payroll(_person([]), _config(), 2026, 8)
    r = calc_payroll(_person([build_event("일반병가", date(2026, 8, 10))]), _config(), 2026, 8)
    assert r.absence_days == 0
    assert r.meal_eligible_days == baseline.meal_eligible_days
    assert r.weekly_holiday_days == baseline.weekly_holiday_days - 1


def test_annual_leave_with_time_is_not_deducted():
    e = build_event("연가", date(2026, 8, 10), time(9, 0), time(12, 0), 180)
    r = calc_payroll(_person([e]), _config(), 2026, 8)
    assert r.late_out_minutes == 0
    assert r.late_out_deduction == 0


if __name__ == "__main__":
    test_full_day_gita_matches_absence()
    test_full_day_gita_removes_that_week_holiday()
    test_partial_gita_deducts_time_only_and_keeps_meal()
    test_partial_gita_does_not_touch_remaining_leave()
    test_early_leave_is_still_offset_not_deducted()
    test_full_day_sick_leave_is_paid_but_loses_weekly_holiday()
    test_annual_leave_with_time_is_not_deducted()
    print("ALL OK")
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest wage_calculator/tests/test_payroll_gita.py -q`
Expected: FAIL — 종일 기타가 `absence_days`에 안 잡히고, 부분시간 기타가 연가로 상계된다.

- [ ] **Step 3: `core/payroll.py`를 고친다**

집계 부분(`public_leave_days` 위아래)을 아래로 바꾼다:

```python
    public_leave_days = sum(e.day_weight for e in period_events if e.classified in ("공가", "유급특별휴가"))
    # 급여·식대를 아예 지급하지 않는 종일 항목(결근·종일 기타, 그리고 확인 화면에서
    # '무급'으로 고른 종일 건). 시간 기재 항목은 일수가 아니라 분 단위로 아래에서 깎는다.
    absence_days = sum(e.day_weight for e in period_events if e.unpaid and not e.is_time_based)
    special_leave_events = [e for e in period_events if e.classified in ("유급특별휴가", "무급특별휴가")]
```

시간 공제·상계 부분(`late_out_events` 아래 `NON_DEDUCTIBLE` 블록 전체)을 아래로 교체한다:

```python
    late_out_events = [e for e in period_events if e.is_time_based]
    # 급여에서 깎을지는 종별 문자열이 아니라 이벤트의 unpaid 축이 정한다.
    #   - 연가/반일연가: 연가 잔량에서 이미 그 분만큼 차감된다(leave_usage_minutes).
    #     급여까지 깎으면 같은 시간을 두 번 받아내는 셈이다.
    #   - 일반병가: 종일 병가가 유급인 것과 같은 기준이어야 한다.
    #   - 확인 화면에서 '유급'으로 고른 건: 유급으로 지정해 놓고 깎으면 앞뒤가 안 맞는다.
    # 조퇴/외출/지각은 보유 연가로 상계된 만큼만 급여에서 빠진다. 기타는 상계하지 않는다.
    offset_covered = sum(
        offset_map.get(id(e), 0)
        for e in late_out_events if e.classified in mapping.OFFSETTABLE
    )
    late_out_minutes = sum(e.minutes for e in late_out_events if e.unpaid) - offset_covered
    offsettable_used = sum(
        e.minutes for e in late_out_events if e.classified in mapping.OFFSETTABLE
    )
    leave_offset_minutes = offset_covered
    leave_shortfall_minutes = offsettable_used - offset_covered
    leave_shortfall = leave_shortfall_minutes > 0
```

파일 상단 import를 고친다:

```python
from . import date_utils, leave_engine, mapping
```

- [ ] **Step 4: 통과를 확인한다**

Run: `python -m pytest wage_calculator/tests/test_payroll_gita.py -q`
Expected: PASS (7 passed)

- [ ] **Step 5: 기존 payroll·소급 테스트를 맞춘다**

Run: `python -m pytest wage_calculator/tests -q -k "payroll or retroactive or end_to_end"`

깨지는 것은 옛 종별 표기를 쓰는 테스트다. 다음 규칙으로 고친다.

- `"조퇴(연가처리)"` → `"연가"`(시간 기재). 의도는 "연가로 처리된 조퇴는 급여에서 안 깎인다"이고, 새 규칙에서 그 표기는 `연가`로 찍힌다.
- `"조퇴(일반병가,진단서미첨부)"` → `"일반병가"`(시간 기재).
- `"병가"` → `"일반병가"`.
- 상계를 기대하는 맨 `"기타"` → `"조퇴"`.
- `test_payroll_time_based_non_deductible.py:test_marked_reasons_are_never_worse_than_bare_early_leave`는 "사유를 정확히 적을수록 손해 보지 않는다"를 고정하는 테스트다. 비교 대상 종별만 위 규칙으로 바꾸고 단정은 그대로 둔다.

- [ ] **Step 6: 커밋**

```bash
git commit -m "feat: 급여·식대 공제를 unpaid 축으로 통일하고 기타의 연가 상계를 제거" -- wage_calculator/core/payroll.py wage_calculator/tests/
```

---

### Task 5: 확인 대상 수집·판정 적용을 `core/pending.py`로 모은다

**Files:**
- Create: `wage_calculator/core/pending.py`
- Test: `wage_calculator/tests/test_pending_decisions.py` (신규)
- Delete(예정, Task 6에서): `webapp/state.py`의 `collect_pending_groups`/`_find_reason_note`, `gui/special_leave_screen.py`의 같은 두 함수

**Interfaces:**
- Consumes: Task 2의 `LeaveEvent.source_row`/`decided`/`unpaid`/`breaks`
- Produces:
  - `pending.PAID_COL = "지급판정"`, `pending.ACCRUAL_COL = "주휴연가판정"`
  - `pending.needs_confirmation(event) -> bool`
  - `pending.collect_groups(people) -> list[dict]` — dict 키: `person_key`, `person_name`, `raw_category`, `start`, `end`, `reason`, `note`, `events`, `row`, `default_paid`, `default_accrual`, `paid`, `accrual`
  - `pending.apply_decisions(groups, decisions) -> None` — `decisions`는 `[{"paid": bool, "accrual": bool}, ...]`
  - `pending.is_decided(groups) -> bool`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`wage_calculator/tests/test_pending_decisions.py`:

```python
import sys
from pathlib import Path
from datetime import date
from types import SimpleNamespace

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

from core import pending
from core.parser import events_from_row


def _person(name, rows):
    events = []
    for row in rows:
        events.extend(events_from_row(row))
    return SimpleNamespace(name=name, events=events)


def _row(category, period, reason="", note="", time_field=None):
    return {
        "소속": "본부", "직급": "기간제", "성명": "홍길동", "생년월일": "1990-01-01",
        "종별": category, "사용기간(날짜)": period, "사용시간(시분)": time_field,
        "사유": reason, "비고": note, "연락처": "", "결재상태": "완료",
    }


def test_unknown_category_is_collected_without_defaults():
    people = {"홍길동::1990-01-01": _person("홍길동", [_row("경조사휴가", "2026-08-10")])}
    groups = pending.collect_groups(people)
    assert len(groups) == 1
    assert groups[0]["raw_category"] == "경조사휴가"
    assert groups[0]["default_paid"] is None
    assert groups[0]["default_accrual"] is None
    assert groups[0]["paid"] is None


def test_known_category_without_reason_is_not_collected():
    people = {"홍길동::1990-01-01": _person("홍길동", [_row("연가", "2026-08-10")])}
    assert pending.collect_groups(people) == []


def test_known_category_with_reason_is_collected_with_rule_defaults():
    people = {"홍길동::1990-01-01": _person("홍길동", [_row("연가", "2026-08-10", reason="집안일")])}
    groups = pending.collect_groups(people)
    assert len(groups) == 1
    assert groups[0]["reason"] == "집안일"
    assert groups[0]["default_paid"] is True       # 연가는 급여를 깎지 않는다
    assert groups[0]["default_accrual"] is True    # 연가는 만근을 깨지 않는다


def test_known_category_with_note_only_is_also_collected():
    people = {"홍길동::1990-01-01": _person("홍길동", [_row("공가", "2026-08-10", note="법원출석")])}
    assert len(pending.collect_groups(people)) == 1


def test_full_day_gita_defaults_are_unpaid_and_breaking():
    people = {"홍길동::1990-01-01": _person("홍길동", [_row("기타", "2026-08-10", reason="개인사정")])}
    groups = pending.collect_groups(people)
    assert groups[0]["default_paid"] is False
    assert groups[0]["default_accrual"] is False


def test_one_group_per_source_row_even_for_multi_day():
    people = {"홍길동::1990-01-01": _person("홍길동", [_row("경조사휴가", "2026-08-10~2026-08-12")])}
    groups = pending.collect_groups(people)
    assert len(groups) == 1
    assert len(groups[0]["events"]) == 3
    assert groups[0]["start"] == date(2026, 8, 10)
    assert groups[0]["end"] == date(2026, 8, 12)


def test_apply_decisions_sets_axes_and_writes_back_to_row():
    row = _row("경조사휴가", "2026-08-10")
    people = {"홍길동::1990-01-01": _person("홍길동", [row])}
    groups = pending.collect_groups(people)
    pending.apply_decisions(groups, [{"paid": False, "accrual": False}])
    event = groups[0]["events"][0]
    assert event.unpaid is True
    assert event.breaks is True
    assert event.classified == "무급특별휴가"
    assert row[pending.PAID_COL] == "무급"
    assert row[pending.ACCRUAL_COL] == "미발생"


def test_apply_decisions_never_overwrites_known_classification():
    """사유가 적힌 연가를 유급으로 확인해도 분류는 '연가'로 남아야 한다 -
    덮어쓰면 연가 잔량 소진이 사라진다."""
    row = _row("연가", "2026-08-10", reason="집안일")
    people = {"홍길동::1990-01-01": _person("홍길동", [row])}
    groups = pending.collect_groups(people)
    pending.apply_decisions(groups, [{"paid": True, "accrual": True}])
    assert groups[0]["events"][0].classified == "연가"
    assert row[pending.PAID_COL] == "유급"


def test_apply_decisions_rejects_wrong_length():
    people = {"홍길동::1990-01-01": _person("홍길동", [_row("경조사휴가", "2026-08-10")])}
    groups = pending.collect_groups(people)
    try:
        pending.apply_decisions(groups, [])
    except ValueError:
        pass
    else:
        raise AssertionError("개수가 다르면 ValueError를 내야 한다")


def test_apply_decisions_rejects_missing_choice():
    people = {"홍길동::1990-01-01": _person("홍길동", [_row("경조사휴가", "2026-08-10")])}
    groups = pending.collect_groups(people)
    try:
        pending.apply_decisions(groups, [{"paid": None, "accrual": True}])
    except ValueError:
        pass
    else:
        raise AssertionError("미선택이면 ValueError를 내야 한다")


if __name__ == "__main__":
    for name, fn in sorted(globals().items()):
        if name.startswith("test_"):
            fn()
    print("ALL OK")
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest wage_calculator/tests/test_pending_decisions.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'core.pending'`

- [ ] **Step 3: `core/pending.py`를 만든다**

```python
"""확인 화면에 올릴 건을 모으고, 사람이 고른 판정을 이벤트·원본 행에 적용한다.

화면(webapp/gui)이 아니라 여기에 두는 이유: 웹 UI와 tkinter GUI 두 곳이 같은
규칙을 써야 하는데, 예전에는 같은 함수가 두 벌 있어 갈라질 위험이 있었다.

확인 화면에 올라오는 건은 두 종류다.
  1) 종별이 9종에 없는 건 - 규칙이 아무것도 정해주지 못하므로 기본값 없이 올린다.
  2) 인식된 종별이지만 사유·비고에 값이 있는 건 - 특별한 사정이 적혀 있을 수
     있으므로 사람이 한 번 본다. 규칙이 정한 값을 기본값으로 채워 올린다.

전월 결과파일에서 판정을 이어받은 건(event.decided)은 다시 묻지 않는다.
"""
from . import mapping

PAID_COL = "지급판정"
ACCRUAL_COL = "주휴연가판정"

PAID_YES = "유급"
PAID_NO = "무급"
ACCRUAL_YES = "발생"
ACCRUAL_NO = "미발생"


def _has_text(value) -> bool:
    return value is not None and str(value).strip() != ""


def needs_confirmation(event) -> bool:
    """이 이벤트를 확인 화면에 올려야 하는가."""
    if event.decided:
        return False
    if mapping.is_pending(event.classified):
        return True
    row = event.source_row or {}
    return _has_text(row.get("사유")) or _has_text(row.get("비고"))


def collect_groups(people) -> list:
    """people(dict[key, TargetPerson]) 전체에서 확인 대상 이벤트를 원본 행 단위로 묶는다.

    원본 B파일 행 1개가 그룹 1개다(여러 날로 펼쳐진 이벤트는 한 그룹). 원본 행
    참조가 없는 이벤트(테스트용으로 직접 만든 경우)는 (사용기간, 종별)로 묶는다.
    """
    groups = {}
    order = []
    for key, person in people.items():
        for e in person.events:
            if not needs_confirmation(e):
                continue
            gkey = (key, id(e.source_row)) if e.source_row is not None else (key, e.source_range, e.raw_category)
            if gkey not in groups:
                row = e.source_row or {}
                known = not mapping.is_pending(e.classified)
                groups[gkey] = {
                    "person_key": key,
                    "person_name": person.name,
                    "raw_category": e.raw_category,
                    "classified": e.classified,
                    "start": e.source_range[0],
                    "end": e.source_range[1],
                    "reason": str(row.get("사유") or ""),
                    "note": str(row.get("비고") or ""),
                    "events": [],
                    "row": e.source_row,
                    # 인식된 종별만 규칙이 답을 갖고 있다. 미인식 종별은 기본값 없이 올린다.
                    "default_paid": (not e.unpaid) if known else None,
                    "default_accrual": (not e.breaks) if known else None,
                    "paid": None,
                    "accrual": None,
                }
                order.append(gkey)
            groups[gkey]["events"].append(e)
    return [groups[k] for k in order]


def apply_decisions(groups, decisions) -> None:
    """사람이 고른 판정을 이벤트의 두 축과 원본 행에 적용한다.

    decisions: [{"paid": bool, "accrual": bool}, ...] - groups와 같은 순서·개수.

    인식된 9종의 classified는 덮어쓰지 않는다. 덮어쓰면 "연가"의 잔량 소진처럼
    종별로만 결정되는 성질이 사라진다. 미인식 종별만 표시용으로 유급/무급
    특별휴가로 바꾼다(비고 문구와 공가 합산에 쓰인다).
    """
    if len(decisions) != len(groups):
        raise ValueError("판정 값 개수가 대기 중인 건수와 맞지 않습니다.")
    for decision in decisions:
        if decision.get("paid") is None or decision.get("accrual") is None:
            raise ValueError("모든 건에 유급/무급과 주휴·연가 발생 여부를 지정해야 진행할 수 있습니다.")

    for group, decision in zip(groups, decisions):
        paid = bool(decision["paid"])
        accrual = bool(decision["accrual"])
        group["paid"] = paid
        group["accrual"] = accrual
        for e in group["events"]:
            e.unpaid = not paid
            e.breaks = not accrual
            e.decided = True
            if mapping.is_pending(e.classified):
                e.classified = "유급특별휴가" if paid else "무급특별휴가"
        row = group["row"]
        if row is not None:
            row[PAID_COL] = PAID_YES if paid else PAID_NO
            row[ACCRUAL_COL] = ACCRUAL_YES if accrual else ACCRUAL_NO


def is_decided(groups) -> bool:
    """모든 건에 판정이 들어갔는가(계산 진행 가능 여부)."""
    return all(g["paid"] is not None and g["accrual"] is not None for g in groups)
```

- [ ] **Step 4: `events_from_row`에 원본 행을 싣는다**

`core/parser.py`의 `events_from_row()` 끝부분 두 곳에 `source_row=row`를 넘긴다:

```python
    events = []
    if time_range is not None:
        t_start, t_end = time_range
        minutes = date_utils.deduct_minutes(t_start, t_end)
        events.append(build_event(raw_category, start_d, t_start, t_end, minutes, source_row=row))
    else:
        for d in date_utils.daterange(start_d, end_d):
            if d.weekday() < 5:
                events.append(build_event(raw_category, d, source_range=(start_d, end_d), source_row=row))
    return events
```

시간 기재 건의 `source_range`는 지금처럼 `build_event`가 `(d, d)`로 채운다.

- [ ] **Step 5: 통과를 확인한다**

Run: `python -m pytest wage_calculator/tests/test_pending_decisions.py -q`
Expected: PASS (10 passed)

`event.decided`가 True인 건을 걸러 내는 동작은 판정 열을 읽는 코드가 있어야 시험할 수 있으므로, 그 테스트는 Task 8에 있다.

- [ ] **Step 6: 커밋**

```bash
git commit -m "feat: 확인 대상 수집·판정 적용을 core/pending으로 분리" -- wage_calculator/core/pending.py wage_calculator/core/parser.py wage_calculator/tests/test_pending_decisions.py
```

---

### Task 6: 웹앱 상태·API를 2항목 판정으로 바꾼다

**Files:**
- Modify: `wage_calculator/webapp/state.py:36-100`(중복 함수 삭제), `:150-175`(`special_leave_groups`, `confirm_special_leave`), `:200-215`(`prepare_calculation`)
- Modify: `wage_calculator/webapp/server.py:19-21`(요청 모델), `:77-84`(라우트)
- Test: `wage_calculator/tests/test_webapp_state_special_leave.py`(갱신), `wage_calculator/tests/test_webapp_server_upload_flow.py`(갱신)

**Interfaces:**
- Consumes: Task 5의 `core.pending`
- Produces:
  - `AppState.special_leave_groups() -> list[dict]` — 항목에 `index`, `person_name`, `raw_category`, `start`, `end`, `reason`, `note`, `default_paid`, `default_accrual`, `paid`, `accrual`
  - `AppState.confirm_special_leave(decisions: list[dict]) -> None`
  - `POST /api/special-leave/confirm` 본문: `{"decisions": [{"paid": true, "accrual": true}, ...]}`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`wage_calculator/tests/test_webapp_state_special_leave.py`를 아래로 교체한다(기존 5개 테스트는 1항목 API 전제라 유지할 수 없다).

```python
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

import openpyxl

from webapp.state import AppState


def _write_a(path):
    wb = openpyxl.Workbook()
    ws = wb.active
    ws.append(["성명", "주민번호", "은행", "계좌번호", "생년월일"])
    ws.append(["홍길동", "900101-1234567", "농협", "123-456", "1990-01-01"])
    wb.save(path)


def _write_b(path, rows):
    wb = openpyxl.Workbook()
    ws = wb.active
    ws.append(["소속", "직급", "성명", "생년월일", "종별", "사용기간(날짜)", "사용시간(시분)", "사유", "비고"])
    for row in rows:
        ws.append(row)
    wb.save(path)


def _state(tmp_path, rows):
    a, b = tmp_path / "a.xlsx", tmp_path / "b.xlsx"
    _write_a(a)
    _write_b(b, rows)
    state = AppState()
    state.load_files(str(a), str(b))
    return state


def test_unknown_category_group_has_no_defaults(tmp_path):
    state = _state(tmp_path, [["본부", "기간제", "홍길동", "1990-01-01", "경조사휴가", "2026-08-10", None, "", ""]])
    groups = state.special_leave_groups()
    assert len(groups) == 1
    assert groups[0]["raw_category"] == "경조사휴가"
    assert groups[0]["default_paid"] is None
    assert groups[0]["default_accrual"] is None


def test_known_category_with_reason_carries_defaults(tmp_path):
    state = _state(tmp_path, [["본부", "기간제", "홍길동", "1990-01-01", "연가", "2026-08-10", None, "집안일", ""]])
    groups = state.special_leave_groups()
    assert len(groups) == 1
    assert groups[0]["reason"] == "집안일"
    assert groups[0]["default_paid"] is True
    assert groups[0]["default_accrual"] is True


def test_known_category_without_reason_is_not_pending(tmp_path):
    state = _state(tmp_path, [["본부", "기간제", "홍길동", "1990-01-01", "연가", "2026-08-10", None, "", ""]])
    assert state.special_leave_groups() == []


def test_confirm_sets_axes(tmp_path):
    state = _state(tmp_path, [["본부", "기간제", "홍길동", "1990-01-01", "경조사휴가", "2026-08-10", None, "", ""]])
    state.confirm_special_leave([{"paid": False, "accrual": False}])
    events = [e for p in state.people.values() for e in p.events]
    assert events[0].unpaid is True
    assert events[0].breaks is True


def test_confirm_rejects_wrong_length(tmp_path):
    state = _state(tmp_path, [["본부", "기간제", "홍길동", "1990-01-01", "경조사휴가", "2026-08-10", None, "", ""]])
    try:
        state.confirm_special_leave([])
    except ValueError:
        return
    raise AssertionError("개수가 다르면 ValueError")


def test_confirm_rejects_missing_choice(tmp_path):
    state = _state(tmp_path, [["본부", "기간제", "홍길동", "1990-01-01", "경조사휴가", "2026-08-10", None, "", ""]])
    try:
        state.confirm_special_leave([{"paid": True, "accrual": None}])
    except ValueError:
        return
    raise AssertionError("미선택이면 ValueError")


def test_prepare_calculation_blocks_when_undecided(tmp_path):
    state = _state(tmp_path, [["본부", "기간제", "홍길동", "1990-01-01", "경조사휴가", "2026-08-10", None, "", ""]])
    try:
        state.prepare_calculation(2026, 8)
    except ValueError as e:
        assert "확인" in str(e)
        return
    raise AssertionError("미확인 건이 있으면 계산을 막아야 한다")
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest wage_calculator/tests/test_webapp_state_special_leave.py -q`
Expected: FAIL — `special_leave_groups()`에 `default_paid` 키가 없다.

- [ ] **Step 3: `webapp/state.py`를 고친다**

1. 파일 상단의 `collect_pending_groups()`와 `_find_reason_note()` **두 함수를 통째로 삭제**한다(각각 `core.pending.collect_groups`와 원본 행 참조로 대체됐다).
2. import를 고친다:

```python
from core import coverage, date_utils, pending
```

`mapping` import는 다른 곳에서 쓰지 않으면 지운다.

3. `_reset_data()`의 `self.pending_leave_groups = []`는 그대로 둔다.
4. `load_files()`의 그룹 수집을 바꾼다:

```python
        self.pending_leave_groups = pending.collect_groups(self.people)
```

5. `special_leave_groups()`를 아래로 교체한다:

```python
    def special_leave_groups(self):
        out = []
        for idx, g in enumerate(self.pending_leave_groups):
            out.append({
                "index": idx,
                "person_name": g["person_name"],
                "raw_category": g["raw_category"],
                "start": g["start"].isoformat(),
                "end": g["end"].isoformat(),
                "reason": g["reason"],
                "note": g["note"],
                "default_paid": g["default_paid"],
                "default_accrual": g["default_accrual"],
                "paid": g["paid"],
                "accrual": g["accrual"],
            })
        return out
```

6. `VALID_SPECIAL_LEAVE_STATUSES`와 `confirm_special_leave()`를 아래로 교체한다:

```python
    def confirm_special_leave(self, decisions):
        """확인 화면의 판정을 적용한다. decisions: [{"paid": bool, "accrual": bool}, ...]"""
        pending.apply_decisions(self.pending_leave_groups, decisions)
```

7. `prepare_calculation()`의 미확인 차단 조건을 바꾼다:

```python
        if not pending.is_decided(self.pending_leave_groups):
            raise ValueError("유급/무급과 주휴·연가 발생 여부 확인이 끝나야 계산을 진행할 수 있습니다.")
```

- [ ] **Step 4: `webapp/server.py`를 고친다**

요청 모델을 바꾼다:

```python
class SpecialLeaveDecision(BaseModel):
    paid: Optional[bool] = None
    accrual: Optional[bool] = None


class SpecialLeaveConfirmRequest(BaseModel):
    decisions: List[SpecialLeaveDecision]
```

라우트 본문을 바꾼다:

```python
    @app.post("/api/special-leave/confirm")
    def confirm_special_leave(req: SpecialLeaveConfirmRequest):
        try:
            state.confirm_special_leave([d.model_dump() for d in req.decisions])
        except ValueError as e:
            raise HTTPException(status_code=400, detail=str(e))
        return {"ok": True}
```

`model_dump()`가 없는 pydantic v1 환경이면 `d.dict()`를 쓴다. 어느 쪽인지는 `python -c "import pydantic; print(pydantic.VERSION)"`으로 확인한다.

- [ ] **Step 5: 통과를 확인한다**

Run: `python -m pytest wage_calculator/tests/test_webapp_state_special_leave.py wage_calculator/tests/test_webapp_server_upload_flow.py wage_calculator/tests/test_special_leave_grouping.py wage_calculator/tests/test_end_to_end_v3.py -q`
Expected: PASS. `test_special_leave_grouping.py`와 `test_end_to_end_v3.py`는 삭제된 `webapp.state.collect_pending_groups`를 import하므로(Task 0에서 그리로 돌려 뒀다), import를 `from core.pending import collect_groups`로 바꾸고 호출부 이름과 반환 dict의 `status` 확인을 `paid`/`accrual`로 고친다.

- [ ] **Step 6: 커밋**

```bash
git commit -m "feat: 확인 화면 판정을 유급/무급 + 주휴·연가 발생 2항목으로 확장" -- wage_calculator/webapp/state.py wage_calculator/webapp/server.py wage_calculator/tests/
```

---

### Task 7: 확인 화면(프론트엔드) 2항목

**Files:**
- Modify: `wage_calculator/webapp/static/js/specialLeave.js`
- Modify: `wage_calculator/webapp/static/js/api.js` (`confirmSpecialLeave` 본문 키)
- Test: 수동 확인(브라우저) + `wage_calculator/tests/test_webapp_static_serving.py`가 그대로 통과

**Interfaces:**
- Consumes: Task 6의 `/api/special-leave` 응답 필드와 `/api/special-leave/confirm` 본문
- Produces: 없음(화면)

- [ ] **Step 1: `api.js`의 확인 호출을 바꾼다**

`confirmSpecialLeave(statuses)`를 찾아 아래로 바꾼다:

```javascript
  confirmSpecialLeave: (decisions) => post("/api/special-leave/confirm", { decisions }),
```

- [ ] **Step 2: `specialLeave.js`를 고친다**

상단 상수를 교체한다:

```javascript
const PAID_LABEL = { null: "미정", true: "유급", false: "무급" };
const ACCRUAL_LABEL = { null: "미정", true: "발생", false: "미발생" };

function nextTri(value) {          // 미정 -> 참 -> 거짓 -> 참 ...
  if (value === null) return true;
  return !value;
}
```

`render()` 안의 초기 상태를 규칙 기본값으로 채운다:

```javascript
  const { groups } = await api.getSpecialLeave();
  const decisions = groups.map(g => ({
    paid: g.default_paid === undefined ? null : g.default_paid,
    accrual: g.default_accrual === undefined ? null : g.default_accrual,
  }));
```

설명 문구를 교체한다:

```javascript
      <p class="screen-subtitle">사람이 판단해야 하는 건입니다. 종별이 목록(연가·반일연가·공가·일반병가·결근·조퇴·외출·지각·기타)에 없거나, 사유·비고에 적힌 내용이 있어 한 번 더 확인이 필요한 건이 올라옵니다.
"종별(원본)" 칸에 근무상황 파일에 적힌 문자열을 그대로 보여주니 이를 보고 판단하세요.
유급이면 일급·식대를 모두 지급하고, 무급이면 둘 다 지급하지 않습니다(결근과 같은 처리). "주휴·연가 발생"을 "미발생"으로 두면 그 주 주휴수당과 그 달 연가가 발생하지 않습니다.
각 칸을 클릭하면 값이 바뀝니다. 두 칸을 모두 지정해야 다음으로 진행됩니다.</p>
```

표 머리를 바꾼다:

```javascript
        <thead><tr><th>성명</th><th>종별(원본)</th><th>기간</th><th>사유(원본)</th><th>비고(원본)</th><th>지급</th><th>주휴·연가 발생</th></tr></thead>
```

`renderRows()`를 교체한다:

```javascript
  function renderRows() {
    tbody.innerHTML = groups.map((g, idx) => {
      const period = g.start === g.end ? g.start : `${g.start}~${g.end}`;
      const { paid, accrual } = decisions[idx];
      const paidCls = paid === true ? "paid" : paid === false ? "unpaid" : "";
      const accrualCls = accrual === true ? "paid" : accrual === false ? "unpaid" : "";
      return `<tr>
        <td>${escapeHtml(g.person_name)}</td>
        <td>${escapeHtml(g.raw_category || "")}</td>
        <td>${escapeHtml(period)}</td>
        <td>${escapeHtml(g.reason)}</td>
        <td>${escapeHtml(g.note)}</td>
        <td><span class="status-pill ${paidCls}" data-idx="${idx}" data-field="paid">${PAID_LABEL[paid]}</span></td>
        <td><span class="status-pill ${accrualCls}" data-idx="${idx}" data-field="accrual">${ACCRUAL_LABEL[accrual]}</span></td>
      </tr>`;
    }).join("");
    nextBtn.disabled = decisions.some(d => d.paid === null || d.accrual === null);
  }
```

클릭 핸들러를 교체한다:

```javascript
  tbody.addEventListener("click", (evt) => {
    const pill = evt.target.closest(".status-pill");
    if (!pill) return;
    const idx = Number(pill.dataset.idx);
    const field = pill.dataset.field;
    decisions[idx][field] = nextTri(decisions[idx][field]);
    renderRows();
  });
```

전송 부분을 교체한다:

```javascript
  nextBtn.addEventListener("click", async () => {
    if (decisions.some(d => d.paid === null || d.accrual === null)) {
      showWarning("모든 건에 지급 여부와 주휴·연가 발생 여부를 지정해야 진행할 수 있습니다.");
      return;
    }
    try {
      await api.confirmSpecialLeave(decisions);
      navigate("targets");
    } catch (e) {
      showError(e.message);
    }
  });
```

ⓘ 안내표(`ensureGuideLoaded` 이하)는 그대로 둔다 — e-사람 안내표는 사람이 판단할 때 여전히 참고가 된다.

- [ ] **Step 3: 브라우저에서 확인한다**

Run: `python wage_calculator/main.py`
확인 항목:
1. 미인식 종별 행은 두 칸 모두 "미정"으로 뜨고 "다음"이 비활성이다.
2. 사유가 적힌 `연가` 행은 "유급"·"발생"이 미리 채워져 있고 "다음"이 바로 활성이다.
3. 칸을 클릭하면 값이 바뀌고 색이 따라 바뀐다.

- [ ] **Step 4: 커밋**

```bash
git commit -m "feat: 확인 화면에 주휴·연가 발생 항목 추가하고 규칙 기본값 표시" -- wage_calculator/webapp/static/js/specialLeave.js wage_calculator/webapp/static/js/api.js
```

---

### Task 8: 판정을 결과 파일에 저장하고 다시 읽는다

**Files:**
- Modify: `wage_calculator/output/raw_sheet.py`
- Modify: `wage_calculator/core/parser.py` (`events_from_row`에 판정 읽기 추가)
- Test: `wage_calculator/tests/test_raw_sheet_decision_columns.py` (신규)

**Interfaces:**
- Consumes: Task 5의 `pending.PAID_COL`/`ACCRUAL_COL`
- Produces: `raw_sheet.COLUMNS`에 `지급판정`·`주휴연가판정` 추가, `raw_sheet.SHEET_NAME` 그대로

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`wage_calculator/tests/test_raw_sheet_decision_columns.py`:

```python
import sys
from pathlib import Path
from datetime import date

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

import openpyxl

from core import pending
from core.parser import events_from_row
from output.raw_sheet import COLUMNS, SHEET_NAME, build_raw_status_sheet


def _row(category, period, **extra):
    row = {
        "소속": "본부", "직급": "기간제", "성명": "홍길동", "생년월일": "1990-01-01",
        "종별": category, "사용기간(날짜)": period, "사용시간(시분)": None,
        "사유": "", "비고": "", "연락처": "", "결재상태": "완료",
    }
    row.update(extra)
    return row


def test_columns_include_decisions():
    assert COLUMNS[-2:] == [pending.PAID_COL, pending.ACCRUAL_COL]


def test_sheet_writes_decision_values(tmp_path):
    row = _row("경조사휴가", "2026-08-10")
    row[pending.PAID_COL] = "무급"
    row[pending.ACCRUAL_COL] = "미발생"
    wb = openpyxl.Workbook()
    wb.remove(wb.active)
    build_raw_status_sheet(wb, [row])
    ws = wb[SHEET_NAME]
    headers = [ws.cell(row=1, column=c).value for c in range(1, len(COLUMNS) + 1)]
    assert headers[-2:] == [pending.PAID_COL, pending.ACCRUAL_COL]
    assert ws.cell(row=2, column=len(COLUMNS) - 1).value == "무급"
    assert ws.cell(row=2, column=len(COLUMNS)).value == "미발생"


def test_events_from_row_reads_decisions():
    row = _row("경조사휴가", "2026-08-10")
    row[pending.PAID_COL] = "무급"
    row[pending.ACCRUAL_COL] = "미발생"
    e = events_from_row(row)[0]
    assert e.decided is True
    assert e.unpaid is True
    assert e.breaks is True
    assert e.classified == "무급특별휴가"


def test_events_from_row_reads_paid_decision():
    row = _row("경조사휴가", "2026-08-10")
    row[pending.PAID_COL] = "유급"
    row[pending.ACCRUAL_COL] = "발생"
    e = events_from_row(row)[0]
    assert e.decided is True
    assert e.unpaid is False
    assert e.breaks is False
    assert e.classified == "유급특별휴가"


def test_known_category_decision_does_not_overwrite_classification():
    row = _row("연가", "2026-08-10", 사유="집안일")
    row[pending.PAID_COL] = "유급"
    row[pending.ACCRUAL_COL] = "발생"
    e = events_from_row(row)[0]
    assert e.classified == "연가"
    assert e.decided is True


def test_partial_decision_is_ignored():
    """한 칸만 채워진 행은 판정으로 인정하지 않고 다시 묻는다."""
    row = _row("경조사휴가", "2026-08-10")
    row[pending.PAID_COL] = "유급"
    e = events_from_row(row)[0]
    assert e.decided is False


def test_decided_row_is_not_collected_again():
    """전월 파일에서 판정을 이어받은 건은 확인 화면에 다시 올리지 않는다."""
    from types import SimpleNamespace

    row = _row("경조사휴가", "2026-08-10")
    row[pending.PAID_COL] = "유급"
    row[pending.ACCRUAL_COL] = "발생"
    person = SimpleNamespace(name="홍길동", events=events_from_row(row))
    assert pending.collect_groups({"홍길동::1990-01-01": person}) == []


def test_undecided_row_is_still_collected():
    row = _row("경조사휴가", "2026-08-10")
    from types import SimpleNamespace

    person = SimpleNamespace(name="홍길동", events=events_from_row(row))
    assert len(pending.collect_groups({"홍길동::1990-01-01": person})) == 1


if __name__ == "__main__":
    for name, fn in sorted(globals().items()):
        if name.startswith("test_") and callable(fn):
            try:
                fn()
            except TypeError:
                pass
    print("ALL OK")
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest wage_calculator/tests/test_raw_sheet_decision_columns.py -q`
Expected: FAIL — `COLUMNS`에 판정 열이 없다.

- [ ] **Step 3: `output/raw_sheet.py`를 고친다**

```python
"""B파일(근무상황) 원본 행을 그대로 옮긴 시트.

이 시트는 단순한 보관용이 아니라 **다음 달 계산의 입력**이다. 다음 달에 이 파일을
'전월 임금내역'으로 첨부하면 core/parser.py가 이 시트를 읽어 지난 달 근무상황을
복원한다(계약 시작월부터 매번 다시 조회해 첨부하지 않아도 되는 이유). 그래서 열
구성을 바꿀 때는 core/parser.load_previous_status_rows()와 같이 고쳐야 한다.

'지급판정'·'주휴연가판정'은 확인 화면에서 사람이 고른 결과다. 규칙만으로 정해진
행은 비워 둔다 - 다음 달에 규칙으로 다시 판정해도 같은 값이 나오므로 저장할 이유가
없고, 비어 있으면 규칙이 바뀌었을 때 새 규칙이 적용된다.
"""
from openpyxl.styles import Font

from core import pending

SHEET_NAME = "근무상황(기간제)"

COLUMNS = [
    "소속", "직급", "성명", "생년월일", "종별", "사용기간(날짜)", "사용시간(시분)",
    "사유", "연락처", "결재상태", "비고", pending.PAID_COL, pending.ACCRUAL_COL,
]
```

`build_raw_status_sheet()`의 본문은 `COLUMNS`를 그대로 돌므로 변경이 필요 없다.

- [ ] **Step 4: `core/parser.py`의 `events_from_row`에 판정 읽기를 붙인다**

`events_from_row()`를 아래로 교체한다:

```python
def events_from_row(row) -> list:
    """B파일 원본 행 1개(dict)를 LeaveEvent 리스트로 변환. 사용 내역이 없는
    (만근) 행이면 빈 리스트. build_target_people()의 매칭 로직과 분리해 두면,
    소급계산(완전퇴사자)처럼 로스터 밖의 임시 인물에게도 같은 파싱 규칙을
    재사용할 수 있다.

    전월 결과파일에서 온 행에는 사람이 이미 고른 판정 두 칸이 들어 있다. 두 칸이
    모두 채워져 있을 때만 판정으로 인정한다(한 칸만 있으면 수기 편집 중일 수
    있으므로 다시 묻는다)."""
    raw_category = row.get("종별")
    date_field = row.get("사용기간(날짜)")
    if raw_category is None or str(raw_category).strip() == "" or date_field is None:
        return []
    raw_category = str(raw_category).strip()

    time_field = row.get("사용시간(시분)")
    time_range = date_utils.parse_time_range(time_field)
    start_d, end_d = date_utils.parse_date_range(date_field)

    events = []
    if time_range is not None:
        t_start, t_end = time_range
        minutes = date_utils.deduct_minutes(t_start, t_end)
        events.append(build_event(raw_category, start_d, t_start, t_end, minutes, source_row=row))
    else:
        for d in date_utils.daterange(start_d, end_d):
            if d.weekday() < 5:
                events.append(build_event(raw_category, d, source_range=(start_d, end_d), source_row=row))

    _apply_saved_decision(row, events)
    return events


def _apply_saved_decision(row, events) -> None:
    """전월 결과파일에 저장된 판정을 이벤트에 되살린다(양쪽 칸이 다 있을 때만)."""
    paid_text = str(row.get(pending.PAID_COL) or "").strip()
    accrual_text = str(row.get(pending.ACCRUAL_COL) or "").strip()
    if paid_text not in (pending.PAID_YES, pending.PAID_NO):
        return
    if accrual_text not in (pending.ACCRUAL_YES, pending.ACCRUAL_NO):
        return
    paid = paid_text == pending.PAID_YES
    accrual = accrual_text == pending.ACCRUAL_YES
    for e in events:
        e.unpaid = not paid
        e.breaks = not accrual
        e.decided = True
        if mapping.is_pending(e.classified):
            e.classified = "유급특별휴가" if paid else "무급특별휴가"
```

`core/parser.py` 상단 import에 두 줄을 추가한다:

```python
from . import date_utils, mapping, pending
```

기존 `from . import date_utils`가 있으면 그 줄을 위 형태로 바꾼다.

- [ ] **Step 5: 통과를 확인한다**

Run: `python -m pytest wage_calculator/tests/test_raw_sheet_decision_columns.py wage_calculator/tests/test_pending_decisions.py -q`
Expected: PASS (18 passed)

- [ ] **Step 6: 커밋**

```bash
git commit -m "feat: 근무상황 시트에 판정 2열 저장·복원" -- wage_calculator/output/raw_sheet.py wage_calculator/core/parser.py wage_calculator/tests/test_raw_sheet_decision_columns.py
```

---

### Task 9: 전월 결과파일에서 근무상황을 읽어 합친다

**Files:**
- Modify: `wage_calculator/core/parser.py` (로더·합치기 추가)
- Test: `wage_calculator/tests/test_parser_status_history.py` (신규)

**Interfaces:**
- Consumes: Task 8의 `raw_sheet.SHEET_NAME`/`COLUMNS`
- Produces:
  - `parser.load_previous_status_rows(path) -> list[dict]`
  - `parser.merge_status_rows(current_rows, previous_rows) -> list[dict]`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`wage_calculator/tests/test_parser_status_history.py`:

```python
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

import openpyxl

from core import pending
from core.parser import load_previous_status_rows, merge_status_rows
from output.raw_sheet import COLUMNS, build_raw_status_sheet


def _row(name, category, period, time_field=None, reason="", paid=None, accrual=None):
    row = {
        "소속": "본부", "직급": "기간제", "성명": name, "생년월일": "1990-01-01",
        "종별": category, "사용기간(날짜)": period, "사용시간(시분)": time_field,
        "사유": reason, "연락처": "", "결재상태": "완료", "비고": "",
    }
    if paid is not None:
        row[pending.PAID_COL] = paid
        row[pending.ACCRUAL_COL] = accrual
    return row


def _save(tmp_path, rows, name="prev.xlsx"):
    wb = openpyxl.Workbook()
    wb.remove(wb.active)
    build_raw_status_sheet(wb, rows)
    path = tmp_path / name
    wb.save(path)
    return str(path)


def test_load_returns_rows_with_same_shape(tmp_path):
    path = _save(tmp_path, [_row("홍길동", "연가", "2026-07-10")])
    rows = load_previous_status_rows(path)
    assert len(rows) == 1
    assert rows[0]["성명"] == "홍길동"
    assert rows[0]["종별"] == "연가"
    assert set(COLUMNS) <= set(rows[0].keys())


def test_load_returns_decision_columns(tmp_path):
    path = _save(tmp_path, [_row("홍길동", "경조사휴가", "2026-07-10", paid="유급", accrual="발생")])
    rows = load_previous_status_rows(path)
    assert rows[0][pending.PAID_COL] == "유급"
    assert rows[0][pending.ACCRUAL_COL] == "발생"


def test_load_missing_sheet_returns_empty(tmp_path):
    wb = openpyxl.Workbook()
    path = tmp_path / "no_sheet.xlsx"
    wb.save(path)
    assert load_previous_status_rows(str(path)) == []


def test_merge_puts_previous_first_then_current():
    prev = [_row("홍길동", "연가", "2026-07-10")]
    cur = [_row("홍길동", "조퇴", "2026-08-10", time_field="15:00~18:00")]
    merged = merge_status_rows(cur, prev)
    assert [r["종별"] for r in merged] == ["연가", "조퇴"]


def test_merge_drops_duplicate_and_keeps_current():
    prev = [_row("홍길동", "연가", "2026-07-10", reason="옛 사유")]
    cur = [_row("홍길동", "연가", "2026-07-10", reason="새 사유")]
    merged = merge_status_rows(cur, prev)
    assert len(merged) == 1
    assert merged[0]["사유"] == "새 사유"


def test_merge_treats_different_person_as_different_row():
    prev = [_row("홍길동", "연가", "2026-07-10")]
    cur = [_row("김철수", "연가", "2026-07-10")]
    assert len(merge_status_rows(cur, prev)) == 2


def test_merge_treats_different_time_as_different_row():
    prev = [_row("홍길동", "조퇴", "2026-07-10", time_field="15:00~18:00")]
    cur = [_row("홍길동", "조퇴", "2026-07-10", time_field="16:00~18:00")]
    assert len(merge_status_rows(cur, prev)) == 2


def test_merge_without_previous_returns_current():
    cur = [_row("홍길동", "연가", "2026-08-10")]
    assert merge_status_rows(cur, []) == cur
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest wage_calculator/tests/test_parser_status_history.py -q`
Expected: FAIL — `ImportError: cannot import name 'load_previous_status_rows'`

- [ ] **Step 3: `core/parser.py`에 로더와 합치기를 추가한다**

파일 상단 import를 고친다:

```python
from output.wage_sheet import COL, SHEET_NAME, DATA_START_ROW
from output.raw_sheet import COLUMNS as STATUS_COLUMNS, SHEET_NAME as STATUS_SHEET_NAME
```

파일 끝에 아래를 추가한다:

```python
def load_previous_status_rows(path) -> list:
    """전월 결과파일의 '근무상황(기간제)' 시트를 B파일과 같은 모양의 행으로 읽는다.

    이 시트는 이 프로그램이 직접 만든 것이라 헤더가 1행에 그대로 있다. 그래도 열
    위치를 고정하지 않고 헤더 텍스트로 찾는다 - 예전 버전이 만든 파일에는 판정
    2열이 없기 때문이다(없으면 그 행은 판정 없이 들어와 확인 화면에서 다시 묻는다).

    시트가 없으면 빈 리스트를 돌려준다. 옛 버전이 만든 파일이나 근무상황을 첨부하지
    않고 만든 파일도 소급용(임금내역 시트)으로는 여전히 쓸 수 있어야 하므로, 여기서
    예외를 던지지 않는다.
    """
    wb = openpyxl.load_workbook(path, data_only=True)
    if STATUS_SHEET_NAME not in wb.sheetnames:
        return []
    ws = wb[STATUS_SHEET_NAME]

    headers = {}
    for c in range(1, ws.max_column + 1):
        v = ws.cell(row=1, column=c).value
        if v is not None:
            headers[str(v).strip()] = c

    rows = []
    for r in range(2, ws.max_row + 1):
        row = {name: None for name in STATUS_COLUMNS}
        for name, col in headers.items():
            row[name] = ws.cell(row=r, column=col).value
        name = row.get("성명")
        if name is None or str(name).strip() == "":
            continue
        row["성명"] = str(name).strip()
        rows.append(row)
    return rows


def _status_row_key(row):
    """중복 판정 키. 사유·비고는 넣지 않는다 - 나중에 채워질 수 있고, 값이
    달라졌다고 다른 근무상황으로 볼 이유가 없다.

    날짜·시간 셀은 openpyxl이 datetime으로 돌려줄 수도, 문자열로 돌려줄 수도 있어
    파싱해 정규화한 뒤 비교한다(정규화 없이 str()로 비교하면 같은 행이 두 번 들어온다).
    """
    birth = row.get("생년월일")
    try:
        birth_key = date_utils.parse_date(birth).isoformat() if birth not in (None, "") else ""
    except (ValueError, TypeError):
        birth_key = str(birth or "").strip()

    period = row.get("사용기간(날짜)")
    try:
        start_d, end_d = date_utils.parse_date_range(period)
        period_key = f"{start_d.isoformat()}~{end_d.isoformat()}"
    except (ValueError, TypeError):
        period_key = str(period or "").strip()

    time_range = None
    try:
        time_range = date_utils.parse_time_range(row.get("사용시간(시분)"))
    except (ValueError, TypeError):
        time_range = None
    time_key = f"{time_range[0]}~{time_range[1]}" if time_range else ""

    return (
        str(row.get("성명") or "").strip(),
        birth_key,
        str(row.get("종별") or "").strip(),
        period_key,
        time_key,
    )


def merge_status_rows(current_rows, previous_rows) -> list:
    """전월 결과파일에서 온 근무상황과 당월 B파일을 하나로 합친다.

    같은 키의 행이 양쪽에 있으면 **당월 B파일이 이긴다** - e-사람에서 방금 조회한
    쪽이 정본이고, 결재·사유가 나중에 채워졌을 수 있다. 순서는 지난 달 -> 당월로
    두어 출력 시트와 산정근거 화면이 시간 순으로 읽히게 한다.
    """
    current_keys = {_status_row_key(r) for r in current_rows}
    merged = [r for r in previous_rows if _status_row_key(r) not in current_keys]
    merged.extend(current_rows)
    return merged
```

- [ ] **Step 4: 통과를 확인한다**

Run: `python -m pytest wage_calculator/tests/test_parser_status_history.py -q`
Expected: PASS (8 passed)

- [ ] **Step 5: 순환 import가 없는지 확인한다**

Run: `python -c "import sys; sys.path.insert(0, 'wage_calculator'); import core.parser, output.raw_sheet, output.build; print('OK')"`
Expected: `OK` — `output/raw_sheet.py`가 `core.pending`을, `core/parser.py`가 `output.raw_sheet`를 import하므로 한 방향인지 확인한다(`core.pending`은 `output`을 import하지 않는다).

- [ ] **Step 6: 커밋**

```bash
git commit -m "feat: 전월 결과파일의 근무상황 시트를 읽어 당월 B파일과 합치기" -- wage_calculator/core/parser.py wage_calculator/tests/test_parser_status_history.py
```

---

### Task 10: 합본을 파이프라인에 배선하고 안내문을 고친다

**Files:**
- Modify: `wage_calculator/webapp/state.py:load_files`
- Modify: `wage_calculator/core/coverage.py:format_gap_message`
- Modify: `wage_calculator/gui/app.py:load_files` (같은 규칙 적용)
- Test: `wage_calculator/tests/test_status_history_roundtrip.py` (신규), `wage_calculator/tests/test_webapp_state_coverage_guard.py`(갱신)

**Interfaces:**
- Consumes: Task 9의 `load_previous_status_rows`, `merge_status_rows`
- Produces: `AppState.giganje_rows`가 합본이 된다(기존 소비자 전부 그대로 동작)

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`wage_calculator/tests/test_status_history_roundtrip.py`:

```python
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

import openpyxl

from core import pending
from output.raw_sheet import SHEET_NAME as STATUS_SHEET_NAME
from webapp.state import AppState


def _write_a(path):
    wb = openpyxl.Workbook()
    ws = wb.active
    ws.append(["성명", "주민번호", "은행", "계좌번호", "생년월일"])
    ws.append(["홍길동", "900101-1234567", "농협", "123-456", "1990-01-01"])
    wb.save(path)


def _write_b(path, rows):
    wb = openpyxl.Workbook()
    ws = wb.active
    ws.append(["소속", "직급", "성명", "생년월일", "종별", "사용기간(날짜)", "사용시간(시분)", "사유", "비고"])
    for row in rows:
        ws.append(row)
    wb.save(path)


def _july_state(tmp_path):
    """7월: 경조사휴가 1건을 무급·미발생으로 확인하고 계산 -> 결과파일 저장."""
    a, b = tmp_path / "a.xlsx", tmp_path / "b_july.xlsx"
    _write_a(a)
    _write_b(b, [["본부", "기간제", "홍길동", "1990-01-01", "경조사휴가", "2026-07-13", None, "", ""]])
    state = AppState()
    state.config_obj.add_survey("테스트조사", "2026-07-01", "2026-09-30")
    state.load_files(str(a), str(b))
    state.confirm_special_leave([{"paid": False, "accrual": False}])
    state.batch_assign(list(state.people.keys()), "테스트조사")
    state.prepare_calculation(2026, 7)
    state.run_calculation()
    return state


def test_july_output_carries_status_and_decision(tmp_path, monkeypatch):
    state = _july_state(tmp_path)
    monkeypatch.setattr("webapp.state.downloads_dir", lambda: str(tmp_path))
    saved = state.download()
    wb = openpyxl.load_workbook(saved[0])
    assert STATUS_SHEET_NAME in wb.sheetnames
    ws = wb[STATUS_SHEET_NAME]
    headers = [ws.cell(row=1, column=c).value for c in range(1, ws.max_column + 1)]
    assert pending.PAID_COL in headers and pending.ACCRUAL_COL in headers
    paid_col = headers.index(pending.PAID_COL) + 1
    assert ws.cell(row=2, column=paid_col).value == "무급"


def test_august_only_b_file_passes_coverage_with_previous_file(tmp_path, monkeypatch):
    state = _july_state(tmp_path)
    monkeypatch.setattr("webapp.state.downloads_dir", lambda: str(tmp_path))
    prev_path = state.download()[0]

    a, b8 = tmp_path / "a.xlsx", tmp_path / "b_aug.xlsx"
    _write_b(b8, [["본부", "기간제", "홍길동", "1990-01-01", "연가", "2026-08-10", None, "", ""]])
    aug = AppState()
    aug.config_obj.add_survey("테스트조사", "2026-07-01", "2026-09-30")
    aug.load_files(str(a), str(b8), prev_path)

    # 7월 건은 판정이 저장돼 있으므로 다시 묻지 않는다.
    assert aug.special_leave_groups() == []
    # 7월 근무상황이 전월 파일에서 채워져 계약 시작월 검증을 통과한다.
    aug.batch_assign(list(aug.people.keys()), "테스트조사")
    aug.prepare_calculation(2026, 8)   # ValueError가 나면 실패
    categories = [row["종별"] for row in aug.giganje_rows]
    assert categories == ["경조사휴가", "연가"]


def test_august_without_previous_file_is_blocked(tmp_path):
    a, b8 = tmp_path / "a.xlsx", tmp_path / "b_aug.xlsx"
    _write_a(a)
    _write_b(b8, [["본부", "기간제", "홍길동", "1990-01-01", "연가", "2026-08-10", None, "", ""]])
    state = AppState()
    state.config_obj.add_survey("테스트조사", "2026-07-01", "2026-09-30")
    state.load_files(str(a), str(b8))
    state.batch_assign(list(state.people.keys()), "테스트조사")
    try:
        state.prepare_calculation(2026, 8)
    except ValueError as e:
        assert "전월" in str(e)
        return
    raise AssertionError("7월이 없으면 계산을 막아야 한다")
```

주의: `_july_state`가 쓰는 `AppState.config_obj.add_survey` 시그니처를 `wage_calculator/core/config.py`에서 먼저 확인하고, 다르면 그 파일의 실제 시그니처에 맞춘다.

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest wage_calculator/tests/test_status_history_roundtrip.py -q`
Expected: FAIL — 8월 계산이 `coverage`에서 막힌다(전월 근무상황이 합쳐지지 않는다).

- [ ] **Step 3: `webapp/state.py`의 `load_files`를 고친다**

```python
    def load_files(self, a_path, b_path, prev_payroll_path=None):
        self._reset_data()
        self.employees = load_employees(a_path)
        current_rows = load_giganje_rows(b_path)
        # 전월 결과파일은 두 가지를 나른다: 소급용 계약기간·실지급액(임금내역 시트)과
        # 지난 달 근무상황+판정(근무상황 시트). 뒤엣것을 당월 B파일과 합쳐 하나로
        # 만들어 두면, 계산 엔진·소급·산정근거 화면은 합본만 보면 된다.
        previous_rows = load_previous_status_rows(prev_payroll_path) if prev_payroll_path else []
        self.giganje_rows = merge_status_rows(current_rows, previous_rows)
        self.people, self.missing_names, self.ambiguous_names = build_target_people(
            self.giganje_rows, self.employees
        )
        self.previous_payroll = load_previous_payroll(prev_payroll_path) if prev_payroll_path else {}
        self.pending_leave_groups = pending.collect_groups(self.people)
        return {
            "ambiguous_names": list(self.ambiguous_names),
            "has_pending_special_leave": bool(self.pending_leave_groups),
        }
```

import에 두 함수를 추가한다:

```python
from core.parser import (
    build_target_people,
    display_label,
    load_employees,
    load_giganje_rows,
    load_previous_payroll,
    load_previous_status_rows,
    merge_status_rows,
)
```

- [ ] **Step 4: `core/coverage.py`의 안내문을 고친다**

`format_gap_message()`의 마지막 문자열을 아래로 바꾼다:

```python
    return (
        "근무상황 파일에 빠진 달이 있어 계산을 진행할 수 없습니다.\n\n"
        f"- 근무상황 파일에서 확인된 달: {', '.join(_fmt(ym) for ym in covered) or '없음'}\n"
        f"- 빠진 달: {', '.join(_fmt(ym) for ym in missing_all)}\n"
        f"- 해당 대상자: {name_text}\n\n"
        "연가(월차) 발생과 주휴수당은 계약 시작일부터 누적으로 판정하기 때문에,\n"
        "빠진 달은 '전원 만근'으로 잘못 처리되어 연가·주휴가 실제보다 많이 잡힙니다.\n\n"
        "다음 중 하나로 채우세요.\n"
        "1) 전월에 이 프로그램으로 만든 임금내역 파일을 '전월 임금내역'으로 함께 첨부합니다\n"
        "   (그 파일의 '근무상황(기간제)' 시트가 지난 달 근무상황을 그대로 담고 있습니다).\n"
        f"2) e-사람에서 {_fmt(earliest)}부터 {_fmt((year, month))}까지 전체 기간의 근무상황을\n"
        "   다시 조회해 첨부합니다."
    )
```

- [ ] **Step 5: 통과를 확인한다**

Run: `python -m pytest wage_calculator/tests/test_status_history_roundtrip.py wage_calculator/tests/test_webapp_state_coverage_guard.py -q`
Expected: PASS. `test_webapp_state_coverage_guard.py`가 안내문 문구를 단정한다면 새 문구에 맞춰 고친다.

- [ ] **Step 6: 전체 테스트**

Run: `python -m pytest wage_calculator/tests -q`
Expected: 전부 PASS. 남는 실패는 옛 종별 표기·옛 API를 쓰는 테스트이므로 Task 4 Step 5, Task 6 Step 5의 규칙으로 고친다.

- [ ] **Step 7: 커밋**

```bash
git commit -m "feat: 전월 결과파일의 근무상황을 합본으로 배선하고 안내문 갱신" -- wage_calculator/webapp/state.py wage_calculator/core/coverage.py wage_calculator/tests/
```

---

### Task 11: 실제 파일로 확인하고 문서를 갱신한다

**Files:**
- Modify: `README.md`, `변경이력.txt`, `임금계산_로직_안내(v5.3).txt`
- Test: 수동 확인(실제 엑셀)

**Interfaces:**
- Consumes: Task 1~10 전부
- Produces: 없음

- [ ] **Step 1: 실제 파일로 두 달 연속 계산을 돌린다**

저장소에 있는 예시 파일을 쓴다.

```bash
python wage_calculator/main.py
```

1. `A_개인정보_입력양식_예시.xlsx` + `B_근무상황_입력양식_예시.xlsx`로 7월 계산 → 결과 저장
2. 저장된 파일을 '전월 임금내역'으로 첨부하고 8월 B파일만 넣어 계산
3. 확인 항목:
   - 7월에 확인한 건이 8월 확인 화면에 다시 나오지 않는다
   - 8월 출력물의 `근무상황(기간제)` 시트에 7·8월 행이 모두 있다
   - 계약 시작월 누락 경고가 뜨지 않는다
   - 산정근거 시트의 연가 원장이 7월분 발생까지 반영한다

- [ ] **Step 2: `변경이력.txt`의 v5.4 항목을 다시 쓴다**

기존 v5.4 줄에서 "종별 인식을 완전일치 표에서 정규화+키워드 규칙으로 전환(청·지청별 표기 차이 수용 — "일반병가", "지각(연가처리)", "공무상병가" 등)" 부분을 아래로 교체하고, 이력 누적 항목을 덧붙인다.

```
v5.4 (예정)        조퇴/외출/지각을 보유 연가로 상계(급여 미공제·연가보상비 감소), 연가 부족 시 계산 팝업 경고, 휴무일 겹친 공휴일 유급휴일 표기 정정,
                   종별을 실제로 쓰이는 9종("연가","반일연가","공가","일반병가","결근","조퇴","외출","지각","기타") 완전일치로 확정 — 그 밖의 표기는 전부 확인 화면으로,
                   "기타"를 조퇴/외출/지각과 분리(연가 상계 없이 급여 공제, 종일이면 결근과 동일하게 식대 미지급·주휴/연가 미발생) — 종일 "기타"가 아무 효과도 없던 문제 수정,
                   확인 화면을 유급/무급 + 주휴·연가 발생 2항목으로 확장하고, 사유·비고가 적힌 인식 종별도 규칙값을 채워 재확인 대상으로,
                   계약 시작월부터 계산월까지 근무상황이 빠진 달이 있으면 계산 차단(빠진 달이 '만근'으로 잘못 처리되던 문제),
                   전월 결과파일의 "근무상황(기간제)" 시트로 지난 달 근무상황과 그 판정을 이어받아, 당월 근무상황만 첨부해도 소급계산이 되도록 개선,
                   시간 기재 항목의 급여 공제 대상 정정 — 조퇴/외출/지각의 연가·급여 이중공제 제거, 일반병가·유급 지정 건은 미공제(사유를 정확히 적을수록 손해 보던 문제)
```

- [ ] **Step 3: `README.md`를 갱신한다**

고칠 곳:
1. 테스트 개수(`자동화 테스트 148개(46개 파일)`)를 실제 값으로. `python -m pytest wage_calculator/tests -q`의 마지막 줄과 `ls wage_calculator/tests/test_*.py | wc -l`로 센다.
1-1. 폴더 구조 설명에서 `gui/` 줄을 지운다(Task 0에서 삭제됐다).
2. 종별 처리 설명이 있는 절에 9종 표를 넣는다(설계 문서 §1 표를 그대로 옮긴다).
3. 입력 파일 설명에 "전월 임금내역 파일이 지난 달 근무상황을 함께 나른다 — 당월 근무상황만 첨부해도 된다"를 추가한다.

- [ ] **Step 4: `임금계산_로직_안내(v5.3).txt`를 갱신한다**

파일 이름의 버전 표기를 `임금계산_로직_안내(v5.4).txt`로 바꾸고(`git mv`), 종별 처리 규칙 절을 9종 표로 교체한다. 확인 화면 설명을 2항목으로 고친다.

- [ ] **Step 5: 전체 테스트 최종 확인**

Run: `python -m pytest wage_calculator/tests -q`
Expected: 전부 PASS. 실패가 하나라도 있으면 커밋하지 않는다.

- [ ] **Step 6: 커밋**

```bash
git commit -m "docs: v5.4 변경이력·README·로직 안내를 9종 규칙과 이력 누적에 맞춰 갱신" -- README.md 변경이력.txt "임금계산_로직_안내(v5.4).txt"
```

---

## 완료 후

전체 테스트 통과와 실제 파일 확인이 끝나면 `superpowers:finishing-a-development-branch`로 master 병합 → exe 빌드(`통계조사관임금계산.spec`) → 배포를 진행한다. 병합·푸시 여부는 사용자에게 묻는다.
