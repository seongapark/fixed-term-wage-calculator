# 통계조사관 임금계산 자동화 프로그램 기획안 (v3)

> 목적: 이 문서를 Claude Code에 전달하여 실행(구현) 단계 진행. 본 문서 자체는 설계만 다룸, 코드 없음.
> 구현 원칙(사용자 확정): 필요한 기능만 구현, 부수적 시나리오 대비 코딩 지양. exe는 완전 로컬 실행(서버/외부 API 없음)이므로 망분리 이슈 없음 — 개발은 개인 노트북(웹망)에서 Claude Code로 진행, 완성된 exe만 업무망으로 이동해서 사용.
> Claude Code 작업 시 유의사항(사용자 표준 선호): 코드는 복붙하기 쉽게 아티팩트로 제공 / 수정 요청 시 처음부터 다시 쓰지 말고 변경된 부분만 반영해 전체 코드 출력 / 필요한 기능 위주로만 작성
> **근무관리 전체 단위는 1분**(날짜·시간 파싱, 조퇴/지각/외출 공제, 연가 발생·사용·잔여 계산 등 모든 계산에서 시간단위 반올림 없이 분단위로 처리)

---

## 1. 배경

- 매월 반복: e-사람 근무상황 export → 엑셀 붙여넣기 → 수식 자동계산 → 임금내역(월중) 양식 산출 → 타부서 전달
- 기존 엑셀은 5개 시트로 구성된 수식 기반 계산기임(`0.2026관리대장`, `1.지급대상&공통입력값`, `2.근무상황추출`, `근무상황(월중)`, `임금내역(월중)`)
- 문제점
  - 매달 수작업 붙여넣기 + 인원(행) 늘어나면 수식 복사 필요
  - 임금내역(월중) 시트의 **주휴(일), 유급휴일(일), 잔여연가(일)** 등이 수식이 아닌 **고정값 하드코딩**으로 사람이 매달 손으로 채워넣는 상태로 확인됨(검증 완료) → 자동화 최우선 대상
  - 조사별로 계약기간이 다르고, 매달 관리대장에 계약기간을 다시 입력하는 게 번거로움
  - 임금 산정 근거를 확인하려면 근무상황(월중) 시트의 복잡한 수식을 직접 추적해야 함

## 2. 목표

- **exe 프로그램 1개**로 다음을 수행:
  1. 개인정보 파일(A) + 근무상황 파일(B) 입력
  2. 급여산정 대상자 목록 자동 도출 → 대상자별 **담당조사 / 근무월** 입력(일괄입력 지원)
  3. 주휴·연가·결근·병가·조퇴외출공제 등 전 계산 자동 수행
  4. **`임금내역(월중)` 시트와 동일한 형식(열 구성/헤더/순서)**의 엑셀 파일 출력(타부서 전달용 고정 양식 — 픽셀 단위 서식까지는 아니어도 열 구성과 헤더명은 원본과 동일하게 유지)
  5. **개인별 임금산정근거를 성명 드롭다운으로 조회**하는 화면 + 동일 내용의 엑셀 탭 출력(검증용)
- 매달 재사용 가능한 고정 템플릿 프로그램으로 유지

## 3. 입력 파일 명세 (2개 파일로 분리)

### 3-A. 개인정보 파일 (A)

| 컬럼 | 내용 |
|---|---|
| 성명 | 매칭 키로 사용 |
| 주민번호 | |
| 은행 | |
| 계좌번호 | |

- 근무시작일/마지막근무일은 이 파일에 넣지 않음 — **계약기간은 4-1 설정탭에서 담당조사별로 일괄등록**한 값을 기본으로 사용(5-5 참고)
- 한 사람은 **하나의 담당조사에만** 배정됨(동시계약 구조상 한 사람이 여러 조사에 겹쳐 배정되는 경우는 없음) → 대상자 확인 화면에서 성명별로 담당조사 1개만 지정

### 3-B. 근무상황 파일 (B)

- 업무망 시스템(e-사람)에서 추출한 원본 그대로 사용(가공 불필요)
- **직급에 "기간제"라는 문자열이 포함된 사람(예: 기간제(통계조사원), 기간제근로자 등)만 임금 산정 대상**. 그 외(예: 통계조사관 등 정규인력)는 같은 파일 안에 섞여 있어도 **자동으로 걸러내고 계산 대상에서 제외**

| 컬럼 | 내용 | 비고 |
|---|---|---|
| 소속 | 부서명 | |
| 직급 | 통계조사관 / 기간제(통계조사원) / 기간제근로자 등 | |
| 성명 | | A파일과 매칭 키 |
| 생년월일 | | |
| 종별 | 결근/공가/연가/반일연가/병가 세부사유 등 17종(5-2 매핑표, 고정) | |
| 사용기간(날짜) | `YYYY-MM-DD` 또는 `YYYY-MM-DD~YYYY-MM-DD` | |
| 사용시간(시분) | `HH:MM~HH:MM` (있으면 시간단위, 없으면 종일) | 분단위까지 정밀 관리 |
| 사유 / 연락처 / 결재상태 / 비고 | | **결재상태는 계산에 사용하지 않음(무시)** |

**핵심 규칙**
- 사용기간에 **시간이 없으면 종일**(연가/결근/공가/병가 등 종별 그대로 1일) 사용
- 사용기간에 **시간이 있으면 해당 시간(분단위)만큼만** 시급/연가 공제

## 4. 설정(Config) 관리 — exe 내 별도 관리 화면

### 4-1. 조사종류 관리 탭
- 입력값: **조사이름**, **조사기간(시작일~종료일)** — 담당조사별로 **일괄등록**
- 신규 조사 추가/기존 조사 수정·삭제 가능
- 용도: 대상자 확인 화면에서 담당조사 선택 시 드롭다운으로 제공 + **선택한 조사의 계약기간(시작일~종료일)이 해당 인원의 계약기간 기본값으로 자동 채워짐**
- 늦은 계약(중도 입사)·중도 퇴사 등 개인별 예외가 있을 수 있으므로, 대상자 확인 화면에서 **인원별로 계약 시작일/마지막일을 수정 가능**하도록 설계(수정 시 해당 인원에게만 적용, 조사 설정값 자체는 변경되지 않음)

### 4-2. 공통 입력값 관리
- 항목: 시급환산 기준, 월 식대, 예산과목 등 매월 바뀔 수 있는 값
- **설정파일(JSON 등)로 저장**, exe 재실행해도 유지
- **프로그램 시작 시 현재 설정값을 팝업/화면으로 표시하여 확인·수정 리마인드**, **임금추출(계산 실행) 버튼 클릭 시에도 동일 값 재확인**(6장 3단계 참고)

### 4-3. 종별 매핑표
- 5-2의 17종 매핑표는 **고정값으로 내장**, 별도 관리 화면 불필요

### 4-4. 공휴일 관리 탭
- 연도별 공휴일 날짜를 **설정탭에서 직접 입력/수정**(자동 조회 없음, 매년 사용자가 직접 등록)
- 용도: 5-4 "유급휴일(일)" 산출 시 계약기간 내 공휴일 수를 이 목록 기준으로 카운트
- **계산 실행 시 확인 대화상자 필수**: 계산 진행 전 "이번 달(급여계산기간) 공휴일: OO월 OO일" 또는 "이번 달 공휴일 없음"을 사용자에게 보여주고 인지시킨 뒤 계속 진행할지 확인받음(등록 누락 방지)

## 5. 계산 로직 명세

### 5-1. 주휴 발생 판정

- 기준: **계약 시작일의 요일**부터 7일을 한 창(window)으로 삼아, 계약기간 끝까지 7일씩 밀어가며 반복 판정
- 각 7일 창에서, 월~금 각 날짜의 **실근무시간(분)**을 먼저 계산한다: 소정근로 480분(8시간)에서 그날의 조퇴/지각/외출 공제분(시간 기재분, 사유가 병가든 연가든 "기타" 등록이든 무관)만 빼고, 전일 결근/전일 병가(시간 미기재)인 날만 0분으로 처리한다. **조퇴/지각/외출은 결근이 아니므로, 8시간을 통째로 비운 게 아닌 이상 그날은 "출근(실근무)한 날"로 계산한다.**
  1. 창의 실제 경과 근무일수(월~금, `NETWORKDAYS`) < 5 → 주휴 미발생(X) (계약 경계로 창 자체가 잘린 경우)
  2. 위에서 계산한 월~금 5일 중 실근무시간이 0분인 날이 1일이라도 있음(결근/종일병가 또는 8시간 전부 조퇴 등으로 공제) → 주휴 미발생(X)
  3. 창의 월~금 실근무시간 **합계가 15시간(900분) 미만** → 주휴 미발생(X)
  4. **근무 연속성**: 창의 마지막 날(계약기간으로 잘리지 않은 경우의 실제 종료일) 다음날에도 계약상 근무가 예정되어 있어야 함 → 창의 종료일이 계약 마지막일과 같다면(=그 다음날 근무 예정이 없음) 주휴 미발생(X). 예: 계약시작 월요일, 계약마지막 금요일(정확히 그 주로 계약 종료)이면 그 주는 주휴 미발생
  5. **공가는 며칠이든(5일 전부 공가인 경우 포함) 주휴에 전혀 영향 없음**(기존 엑셀에 있던 "5일 전부 공가면 미발생" 예외 조건은 제거) — 공가일은 실근무시간 계산에서도 공제 없이 그대로 480분 근무한 것으로 취급
  6. 위 조건에 모두 해당 없으면 → 주휴 발생(O), 지급월 귀속은 창 종료일 기준 월로 판단(당월/익월 구분)
- **인원/계약기간 상관없이 임의 개수의 7일 창을 반복 처리할 수 있는 함수로 일반화**(기존 엑셀은 창 6개 고정 열복사 방식)

### 5-2. 종별 → 분류 매핑표 (17종, 고정)

| 종별(원본) | 분류 |
|---|---|
| 결근 | 결근 |
| 공가 | 공가 |
| 사망(본인 및 배우자의 조부모·외조부모) | 공가 |
| 자녀돌봄휴가 | 공가 |
| 기타 | 기타 |
| 반일연가(오후) | 반일연가 |
| 반일연가(오전) | 반일연가 |
| 조퇴(일반병가,진단서미첨부) | 병가 |
| 지각(일반병가,진단서미첨부) | 병가 |
| 외출(일반병가,진단서미첨부) | 병가 |
| 일반병가(진단서미첨부) | 병가 |
| 연가 | 연가 |
| 외출(연가) | 연가 |
| 조퇴(연가) | 연가 |
| 일반병가(진단서첨부) | 병가 |
| 지각(일반병가,진단서첨부) | 병가 |

- 반일연가는 0.5일로 카운트, 그 외 종일 항목은 1일로 카운트
- 시간이 기재된 항목(조퇴/외출/지각)은 위 분류와 별개로 **분단위 공제** 추가 적용(점심시간 포함 판단: 12시 이전 시작 & 13시 이후 종료면 점심 1시간 자동 차감)

**⚠️ 용어 정리(혼동 방지)**
- **병가**: "아파서 하루 종일 근무를 쉬는 것"(사유). 위 매핑표에서 시간 미기재 항목("일반병가(진단서미첨부/첨부)")만 이에 해당
- **조퇴/지각/외출**: "늦게 출근/일찍 퇴근/근무 중 나갔다 옴"(행동). 사유가 병가(몸이 아파서)든 연가(개인사유)든 상관없이, **시간이 기재된 경우는 전부 조퇴/지각/외출 규칙**을 따름 — 위 매핑표에서 이 항목들이 "병가"로 분류되어 있는 건 어디까지나 일수 집계상의 편의 분류일 뿐, 5-1(주휴)·5-3(만근) 판정에서는 "전일 병가"와 별개로 취급함

### 5-3. 연가(월차) 발생·사용·잔여 산정

**발생 규칙**
- 계약 시작일 기준, **입사월과 동일한 날짜의 전날까지를 1개월 만근 구간**으로 봄
  - 예: 계약 시작이 2일이면 → 1구간 = [당월 2일, 익월 1일], 하루도 안 빠지고 만근해야 연가 1개 발생
  - 발생 시점: 구간 종료 다음날(=다음 구간 시작일)부터 사용 가능
- 이후 구간도 1개월 단위로 반복, 매 만근 구간마다 연가 1개씩 누적 발생
- **만근을 깨는 사유(확정)**
  - **결근(전일)** 발생 시 → 만근 깨짐(해당 구간 연가 미발생)
  - **병가를 하루 종일(전일) 사용**한 경우 → 만근 깨짐
  - **조퇴/지각/외출**(시간 기재분, 사유가 병가든 연가든 무관)이 **1건이라도(1분이라도)** 있으면 → 만근 깨짐
  - **공가**는 전일이든 시간 단위든 상관없이 만근에 **영향 없음**(만근 유지)
  - **"기타" 종별도 만근에 영향 없음**(만근 유지)
- 즉 **5-1(주휴) 판정 조건과 완전히 동일**함(둘 다: 결근/전일병가/조퇴·지각·외출 1분 = 미발생·만근깨짐, 공가는 무관) — 구현 시 동일 판정 함수를 공유해도 무방

**사용(차감) 규칙**
- 연가 사용은 **분단위**로 관리(하루 8시간=480분 기준 환산), 사용 시마다 발생 연가 잔량에서 사용한 분만큼 차감

**잔여 정산(연가보상비)**
- 계약 마지막 날 기준: 누적 발생 연가 − 누적 사용 연가 = 잔여 연가(분단위 보유 가능)
- 잔여 연가를 일 단위로 환산(잔여분 ÷ 480분)하여 `(일급 + 하루식대) × 잔여연가(일, 소수 가능)`로 연가보상비 산출

### 5-4. 급여 계산

| 항목 | 산출 방식 |
|---|---|
| 일급 | 시급환산값 × 8 |
| 실출근(일) | `NETWORKDAYS(계약시작,계약마지막) - 공가일수 - (유급휴일+결근)` |
| 공가(일) | 근무상황 집계값 |
| 유급휴일(일) | 4-4 설정탭 공휴일 목록 중 계약기간 내 포함된 날짜 수(자동 산출) |
| 결근(일) | 근무상황 집계값(자동 산출) |
| 계(일) | 실출근 + 유급휴일 + 결근 |
| 조퇴외출(시간) | 근무상황 집계값(시간단위 종별의 합, 내부적으로는 분단위로 정밀 관리하되 임금내역 표시값은 가독성을 위해 시간(소수 2자리)으로 환산) |
| 주휴(일) | 5-1 로직 결과 자동 반영 |
| 식대해당일 | 급여계산기간 일수 − 결근일수 |
| 잔여연가(일) | 5-3 로직 결과 자동 반영 |
| 급여액 | 일급 × 계(일) |
| 조퇴외출공제 | `ROUNDDOWN(시급 × 조퇴외출시간, -1)` |
| 기본급 | `ROUNDDOWN(급여액 − 조퇴외출공제, -1)` |
| 주휴수당 | 일급 × 주휴(일) |
| 정액급식비 | `ROUNDDOWN(월식대 × 식대해당일/월력상일수, -1)` |
| 연가보상비 | (일급 + 하루식대) × 잔여연가(일) |
| 지급총액 | `ROUNDDOWN(기본급+주휴수당+정액급식비+연가보상비, -1)` |

### 5-5. 조사종류별 계약기간 관리

- 담당조사는 4-1 설정탭 목록에서 선택
- 계약기간(계약 시작일/마지막일)은 **선택한 조사의 설정탭 등록값을 기본으로 자동 채움**
- 중도 입사·중도 퇴사 등 개인별 예외가 있으면 **대상자 확인 화면에서 해당 인원의 계약 시작일/마지막일만 개별 수정** 가능(수정값이 이후 모든 계산의 기준이 됨)
- 급여계산기간(해당 월의 시작/마지막일)은 **월 1일~월말 또는 계약종료일 중 빠른 날** 기준으로 산출

## 6. 화면 흐름 (GUI, tkinter 기준)

0. **설정 화면** (최초 실행 시 자동 표시 + 메뉴에서 언제든 접근 가능)
   - 조사종류 관리(조사이름/조사기간 추가·수정·삭제)
   - 공통 입력값(시급, 월식대 등) 확인 및 수정 — **매 실행 시 리마인드**
   - 공휴일 목록 관리(4-4)
1. **파일 업로드 화면**
   - 개인정보 파일(A) 선택
   - 근무상황 파일(B) 선택
2. **대상자 확인 화면**
   - A파일 기준 급여산정 대상자 목록 표시(성명 등 인적사항), **체크박스로 다중 선택** 가능
   - 담당조사(4-1 목록 드롭다운) 선택 → 계약 시작일/마지막일이 설정탭 값으로 자동 채워짐, 근무월 입력
   - **일괄 매칭**: 여러 명을 체크박스로 선택한 뒤 담당조사 1개를 지정하면 선택된 인원 전체에 한 번에 매칭(하나하나 개별 매칭할 필요 없음) — 한 사람은 담당조사 1개만 가짐
   - **계약 시작일/마지막일은 인원별로 수정 가능**(중도 입사·중도 퇴사 등 예외 반영)
3. **계산 실행**
   - **진행 전 확인 대화상자**: 아래 내용을 함께 표시 → 사용자가 확인 후 [계속] 눌러야 계산 진행
     - "이번 달(급여계산기간) 공휴일: OO월 OO일" 또는 "이번 달 공휴일 없음"(공휴일 등록 누락 방지)
     - "현재 적용 시급: OOO원 / 월 식대: OOO원"(4-2 공통 입력값이 이번 달 기준으로 맞는지 확인)
     - **"계약기간을 개별 수정한 인원: [성명 목록]"**(2단계에서 설정탭 기본값을 손으로 바꾼 사람이 있으면 명단으로 재확인, 없으면 "없음")
   - 진행 버튼 → 5장 로직에 따라 전원 일괄 계산
   - 오류(종별 매핑표에 없는 사유, 계약기간 밖 근무상황 등) 발생 시 사람별로 경고 표시
   - **B파일(근무상황)에서 직급에 "기간제"가 포함된 행만 걸러낸 뒤(3-B 참고), 그중 A파일(개인정보)에 없는 성명이 발견되면 경고창을 띄우고 임금 산정을 진행하지 않음**(A파일 보완 후 재시도 필요). 정규인력(통계조사관 등)은 애초에 필터링 단계에서 제외되므로 경고 대상 아님
4. **결과 화면**
   - 계산 결과(임금내역) 미리보기
   - 다운로드: 최종 엑셀 파일(임금내역 + 산정근거 탭 포함)
   - **파일명**: `'{YY}년 {M}월 고용노동통계조사관 임금 내역` 템플릿(예시: `'26년 7월 고용노동통계조사관 임금 내역`) — **2단계에서 입력한 급여산정 연/월 값으로 매번 자동 치환**(고정 문자열 아님, 하드코딩 금지)
   - **저장 위치**: 사용자 다운로드 폴더에 자동 저장
5. **산정근거 확인 화면** (신규)
   - **성명 드롭다운**으로 대상자 선택 → 선택한 1명에 대해 아래 4개 산정근거 표를 화면에 표시(7장 상세 참고)
     1. 주휴 산정근거
     2. 조퇴외출 합 산정근거
     3. 식대해당일 산정근거
     4. 잔여연가 산정근거
   - 결과 화면(4번)에서 사람 클릭 시 바로 이 화면으로 이동하는 방식도 가능

## 7. 출력 명세

### 7-0. 임금내역(월중) 시트 (형식 고정, 타부서 전달용)
- 기존 `임금내역(월중)` 시트의 **열 구성·헤더·순서를 그대로 재현**(타 부서에 그대로 전달하는 양식이므로 임의 변경 금지)
- 정확한 셀 병합·글꼴 등 픽셀 단위 서식까지 완전히 동일할 필요는 없으나, **열 구성/헤더명/데이터 배치는 원본과 동일**해야 함
- 값은 전부 계산 결과로 채움(수식이 아니어도 무방 — exe 결과물이므로 값으로 출력)

### 7-1. 주휴 산정근거
7일 창 단위 표: `[창번호 | 시작일 | 종료일 | 근무일수 | 결근일수 | 공가일수 | 병가일수 | 판정(O/X) | 미발생 사유]`
- 미발생 사유는 5-1의 어느 조건에 걸렸는지 텍스트로 명시(예: "근무일수 5일 미만", "실근무 없는 날 발생(결근·종일병가 또는 8시간 전부 공제)", "주 실근무시간 15시간 미만", "근무 연속성 없음(계약 마지막 주)" 등)

### 7-2. 조퇴외출 합 산정근거
시간이 기재된 근무상황 원본행 나열: `[날짜 | 종별 | 시작시간 | 종료시간 | 점심포함여부 | 공제시간(분)]`
- 하단에 합계행: 총 공제시간(분) = 임금내역의 "조퇴외출(시간)" 값 × 60 (임금내역은 시간(소수)으로 환산 표시되므로 분단위와 직접 비교 시 60을 곱해 환산)

### 7-3. 식대해당일 산정근거
`[급여계산기간 시작 | 급여계산기간 종료 | 기간 총 일수 | 결근일수 | 식대해당일 = 총일수 − 결근일수]`

### 7-4. 잔여연가 산정근거
만근구간 단위 표: `[구간번호 | 구간시작 | 구간종료 | 만근여부 | 연가발생(1/0) | 해당구간 사용내역(날짜/분) | 구간종료시 누적잔여(분)]`
- 하단 요약: 최종 잔여연가(분) → 일 환산값 → 연가보상비 계산식

## 8. 기술 스택 (배포 환경 고려)

- Python + PyInstaller → 단일 exe (기존 HWPX 자동화 프로그램과 동일한 배포 방식)
- GUI: tkinter
- 엑셀 입출력: openpyxl
- 설정 저장: JSON 설정파일(조사종류 목록, 공통입력값)
- 완전 로컬 파일 입출력만 사용 — 서버/DB/외부 API 없음 → 망분리 이슈 없음(exe만 업무망으로 이동해서 사용)
- 구현 시 꼭 필요한 기능 위주로만 작성(다양한 예외 시나리오 대비 코드는 지양)

## 9. 확정 사항 정리 (구현 착수 가능)

- 연가 발생을 깨는 사유: **결근(전일) 또는 병가(전일 사용) 또는 조퇴/지각/외출(1분이라도)만 해당, 공가는 무관** — 5-1과 5-3 규칙 동일하게 통일 완료
- 한 사람은 담당조사 1개만 가짐(다중 조사 배정 케이스 없음) — 3-A, 5-5, 6장 반영 완료
- 공휴일: 설정탭에서 직접 입력·관리, 계산 실행 전 확인 대화상자로 사용자 인지 — 4-4, 6장 반영 완료
- 결재상태: 계산에 사용하지 않음(무시) — 3-B 반영 완료
- "기타" 종별: 주휴·만근 판정에 영향 없음 — 5-1, 5-3 반영 완료

## 10. 다음 단계

- 모든 확인 필요 사항이 정리됨 → 본 문서를 Claude Code 세션에 전달하여 구현 착수
